# 手机端响应式优化说明（工单系统）

## 1. 这次做了什么（给产品和开发都能看懂的版本）

你可以把这次优化理解为：  
原来页面像“只给大屏电脑设计的表格”，在手机上会挤在一起；  
现在改成了“手机也能完整点单的外卖页面”——菜单能打开、筛选能填、表格能滑、按钮能点。

本次主要优化两块：

1. `miduo-frontend/src/layouts/MainLayout.vue`  
   - 手机端改成抽屉菜单（从左侧滑出），不再长期占屏遮挡内容。
   - 顶部区域做了手机压缩：隐藏搜索框、隐藏用户名文本、保留关键操作按钮。
   - 通知抽屉在手机端自动变窄（`92vw`），避免超出屏幕。

2. `miduo-frontend/src/views/ticket/TicketListView.vue`  
   - 手机端把“多标签页切换”改成下拉选择，避免文字挤爆。
   - 筛选表单改成单列布局，输入框 100% 宽度。
   - 查询/重置按钮在手机端做并排等宽，手指更容易点中。
   - 表格保留横向滑动能力，确保“操作列（详情）”和所有字段都可访问。

---

## 2. 功能用途

### 2.1 手机端菜单抽屉
- **用途**：在窄屏中也能打开完整导航并进入任意模块。
- **用户价值**：不需要缩放页面，也不会被左侧菜单遮住内容。

### 2.2 手机端工单筛选区
- **用途**：在手机上完整输入工单编号、标题、分类、状态、优先级、时间范围并查询。
- **用户价值**：筛选字段不丢失、不重叠、可直接操作。

### 2.3 手机端工单表格可操作
- **用途**：通过左右滑动查看完整列，并点击“详情”进入工单详情。
- **用户价值**：保证“能看到 + 能点到 + 能进入下一步”。

---

## 3. 使用方法（验收步骤）

## 3.1 本地启动
```bash
cd /workspace/miduo-frontend
npm install
npm run dev
```

## 3.2 手机端验收（推荐 DevTools 设备模式）
1. 切到 375px / 390px / 414px 宽度。
2. 打开左上角菜单按钮，确认抽屉可展开并可跳转页面。
3. 进入“所有工单”：
   - 使用筛选条件后点击“查询”。
   - 点击“重置”后条件恢复。
4. 在表格区域左右滑动，确认可看到完整列和“详情”按钮。
5. 点击“详情”进入工单详情页。
6. 点击顶部通知按钮，确认通知抽屉在手机上不溢出屏幕。

---

## 4. 参数说明（本次改动相关）

## 4.1 MainLayout.vue

| 参数/状态 | 类型 | 说明 |
|---|---|---|
| `MOBILE_BREAKPOINT` | `number` | 移动端判断阈值，当前为 `768` |
| `isMobile` | `boolean` | 当前是否处于移动端宽度 |
| `mobileSidebarVisible` | `boolean` | 手机端菜单抽屉显示状态 |
| `notificationDrawerVisible` | `boolean` | 通知抽屉显示状态 |

## 4.2 TicketListView.vue

| 参数/状态 | 类型 | 说明 |
|---|---|---|
| `MOBILE_BREAKPOINT` | `number` | 移动端判断阈值，当前为 `768` |
| `isMobile` | `boolean` | 决定 tabs/select、表单布局、操作列固定策略 |
| `query` | `TicketPageInput` | 工单列表查询参数对象 |
| `timeRange` | `string[]` | 时间区间筛选 |

---

## 5. 返回值说明（核心方法）

> 这里的“返回值”按前端方法来解释，便于联调和阅读。

| 方法 | 返回值 | 说明 |
|---|---|---|
| `handleSidebarTrigger()` | `void` | 手机端打开抽屉；桌面端折叠/展开侧栏 |
| `handleMenuSelect(index)` | `void` | 跳转菜单路由；手机端自动收起抽屉 |
| `updateViewportState()` | `void` | 根据窗口宽度更新 `isMobile` |
| `handleTabChange(value)` | `void` | 切换工单视图（我创建/我待办/所有工单等） |
| `handleSearch()` | `void` | 带当前筛选条件重新请求列表 |
| `handleReset()` | `void` | 清空筛选条件并重新请求列表 |

---

## 6. 常见问题（含恢复建议）

### Q1：`npm run build` 报错 `vue-tsc: not found`
- **检测**：构建命令直接报找不到 `vue-tsc`。
- **记录（错误类型）**：依赖缺失 / 环境未安装 node_modules。
- **恢复建议**：
  1. `cd /workspace/miduo-frontend`
  2. `npm install`
  3. 重新执行 `npm run build`

### Q2：TypeScript 报 `Parameter 'value' implicitly has an 'any' type`
- **检测**：`TicketListView.vue` 中 `@change="(value) => ..."` 触发类型检查错误。
- **记录（错误类型）**：TS 隐式 any 类型错误（TS7006）。
- **恢复建议**：改为 `@change="handleTabChange"`，复用已声明的函数签名。

---

## 7. 示例截图（终端运行效果）

> 以下为本次构建成功的终端输出节选，可作为“已可编译”的证据截图文本版。

```text
> miduo-frontend@0.0.0 build
> vue-tsc -b && vite build

vite v7.3.1 building client environment for production...
✓ 1700 modules transformed.
✓ built in 8.12s
```

---

## 8. 版本历史

| 版本 | 变更内容 |
|---|---|
| `v1.0.0-mobile-responsive` | 新增手机端菜单抽屉、工单列表单列筛选、移动端表格可操作优化 |
| `v1.0.1-mobile-build-fix` | 修复 TS7006（隐式 any）并完成前端构建验证 |
| `v1.1.0-ticket-accept-auto-claim` | 修复“点击受理后基础信息处理人不变”问题：受理动作默认自动认领当前操作人 |
| `v1.1.3-ticket-detail-iphone14pm-layout` | 优化工单详情页在 iPhone 14 Pro Max 布局：双栏改单栏、操作区换行、表单与弹窗适配 |
| `v1.1.5-kanban-error-message-zh` | 工单看板流转报错文案优化：将状态码（如 pending_accept）统一转换为中文状态名（如 待受理） |

---

## 9. 工单受理自动认领修复说明（后端）

### 9.1 功能用途
- **用途**：当处理人点击“受理”时，系统自动把“处理人”更新为当前点击人。
- **类比理解**：像“接单按钮”，谁点了“接单”，配送单就挂到谁名下；不会出现“单子开始配送了，但负责人还显示别人”。

### 9.2 使用方法（联调/验收）
1. 打开任意处于待受理节点的工单（如：`pending_accept` / `pending_test_accept` / `pending_dev_accept`）。
2. 用用户A点击“受理”。
3. 刷新工单详情页，检查右侧“基础信息 -> 处理人”。
4. 预期结果：处理人应更新为用户A，且状态进入下一节点（如处理中/测试中/开发中）。

### 9.3 参数说明（本次修复相关）
| 参数 | 类型 | 说明 |
|---|---|---|
| `TransitInput.newAssigneeIds` | `List<Long>` | 流转时手动指定处理人列表（优先级最高） |
| `TransitInput.newAssigneeId` | `Long` | 流转时手动指定单个处理人 |
| `operatorId` | `Long` | 当前操作用户ID；本次修复在“受理”且未显式指定处理人时，自动写入该用户 |

### 9.4 返回值说明（接口行为）
| 接口 | 返回值 | 说明 |
|---|---|---|
| `PUT /api/ticket/transit/{id}` | `ApiResult<Void>` | 成功时返回空数据；本次修复后会同步更新工单主处理人 |

### 9.5 常见问题（新增）
#### Q3：为什么工单状态变成“处理中”了，但处理人还是旧人？
- **检测**：查看流转记录中动作为“受理”，但详情页处理人未变化。
- **记录（错误类型）**：状态流转与处理人认领未联动（业务逻辑缺失）。
- **恢复建议**：
  1. 升级到版本 `v1.1.0-ticket-accept-auto-claim` 及以上；
  2. 重新执行“受理”动作；
  3. 若仍异常，检查该工作流动作名/状态映射是否被自定义改动（受理动作需能被识别）。

---

## 10. Bug简报“确认通过”校验报错修复（后端）

### 10.1 功能用途
- **用途**：修复 Bug 简报在点击“确认通过”时因参数校验过严导致的 9999 报错。
- **类比理解**：以前像“通过申请也必须写长篇理由”，现在改成“通过可以一句话或不写，驳回仍然必须写清楚原因”。

### 10.2 使用方法（联调/验收）
1. 打开一条“待审核”的 Bug 简报详情。
2. 在“审核通过”弹窗中输入短意见（例如“通过”）或留空。
3. 点击“确认通过”。
4. 预期结果：接口成功返回，状态进入“已归档”，不再出现 `MethodArgumentNotValidException`。

### 10.3 参数说明（本次修复相关）
| 接口 | 参数 | 类型 | 说明 |
|---|---|---|---|
| `PUT /api/bug-report/approve/{id}` | `reviewComment` | `String` | **可选**，审核通过意见，不再要求最少 10 个字符 |
| `PUT /api/bug-report/reject/{id}` | `reviewComment` | `String` | **必填且最少 10 个字符**，用于保证驳回理由完整 |

### 10.4 返回值说明（接口行为）
| 接口 | 返回值 | 说明 |
|---|---|---|
| `PUT /api/bug-report/approve/{id}` | `ApiResult<Void>` | 成功时返回空数据，简报状态流转为“已归档” |
| `PUT /api/bug-report/reject/{id}` | `ApiResult<Void>` | 成功时返回空数据，简报状态流转为“已驳回” |

### 10.5 常见问题（新增）
#### Q4：为什么“确认通过”还会提示参数校验失败？
- **检测**：查看返回是否仍包含 `Validation failed for argument`。
- **记录（错误类型）**：后端版本未更新到本次修复代码，或请求仍走旧接口定义。
- **恢复建议**：
  1. 升级到版本 `v1.1.1-bugreport-approve-validation-fix` 及以上；
  2. 重新部署后端并清理前端缓存再试；
  3. 如果是“驳回”操作，请确认审核意见至少 10 个字符。

### 10.6 示例截图（终端运行效果）
```text
[INFO] BUILD SUCCESS
```

### 10.7 版本历史（新增）
| 版本 | 变更内容 |
|---|---|
| `v1.1.1-bugreport-approve-validation-fix` | 修复 Bug 简报“审核通过”时 reviewComment 校验过严导致的 MethodArgumentNotValidException；通过与驳回分离校验规则 |

---

## 11. 所有工单页：背景统一 + 筛选区紧凑化（前端）

### 11.1 功能用途
- **用途1：统一背景色**  
  解决“左边白、右边灰”的视觉割裂问题，让筛选区和列表区看起来像同一张工作台。
- **用途2：筛选区横向排列**  
  把原来“竖着一列”的筛选条件改成“横向自动换行”，减少左右大面积空白。
- **用途3：按钮尺寸合理**  
  查询/重置按钮改为常规宽度，不再出现“按钮撑满一大条”的情况。

### 11.2 使用方法（验收步骤）
1. 打开“所有工单”页面（桌面端宽度 >= 1024）。
2. 观察筛选区：应为横向多列排列，空间利用更紧凑。
3. 观察查询/重置按钮：应为正常按钮宽度，不是整行超宽按钮。
4. 检查页面左右视觉：不应再出现“左白右灰”的明显割裂。
5. 把窗口缩小到手机宽度（<=768）：筛选项应自动切回单列，按钮并排等宽，保证可点按。

### 11.3 参数说明（本次改动相关）
| 参数/样式 | 类型 | 说明 |
|---|---|---|
| `.ticket-list-page` | CSS class | 改为纵向容器并统一背景承载，避免筛选卡片偏到右侧 |
| `.query-form` | CSS class | 使用 `flex + wrap`，让筛选项横向排布并自动换行 |
| `.query-input` | CSS class | 输入框统一宽度，避免忽宽忽窄 |
| `.query-form-actions` | CSS class | 操作按钮区靠右对齐 |
| `.query-action-buttons .el-button` | CSS rule | 按钮设置最小宽度，避免过宽 |

### 11.4 返回值说明（交互行为）
| 操作 | 返回值 | 说明 |
|---|---|---|
| 点击“查询” | `void` | 触发列表刷新，按当前横向筛选条件生效 |
| 点击“重置” | `void` | 清空筛选并刷新列表 |

### 11.5 常见问题（新增）
#### Q5：为什么我看到筛选区还是竖着一列？
- **检测**：确认页面是否是旧缓存版本（样式文件未刷新）。
- **记录（错误类型）**：前端静态资源缓存导致的样式未更新。
- **恢复建议**：
  1. 强制刷新浏览器（`Ctrl+F5`）；
  2. 重新启动前端开发服务；
  3. 确认已升级到版本 `v1.1.2-ticket-list-layout-polish` 及以上。

### 11.6 版本历史（新增）
| 版本 | 变更内容 |
|---|---|
| `v1.1.2-ticket-list-layout-polish` | 优化“所有工单”页：统一背景视觉、筛选区横向紧凑排布、查询按钮宽度收敛 |

---

## 12. 工单详情页：iPhone 14 Pro Max 布局优化（前端）

### 12.1 功能用途
- **用途1：把双栏改成手机友好的单栏**  
  以前像“把两张并排海报硬塞进手机屏幕”，文字会被挤坏；现在改成“上下一张张看”，信息更清楚。
- **用途2：操作按钮不打架**  
  顶部按钮区域支持自动换行并统一触控高度，避免在 430px 宽度下重叠或错位。
- **用途3：弹窗不超屏**  
  各类弹窗宽度改为 `92vw` 上限规则，避免 iPhone 机型上左右被裁切。

### 12.2 使用方法（验收步骤）
1. 进入工单详情页（建议用 DevTools 设备模式，选择 iPhone 14 Pro Max 或 430x932）。
2. 检查“详细信息 + 右侧信息面板”布局：
   - 预期：主内容在上，信息面板在下（单列）。
3. 检查顶部操作区（重新打开/转派/关注等）：
   - 预期：按钮可换行，点击区域足够大，不重叠。
4. 打开“企微解析 / 分派 / 转派 / 工单操作 / 关闭工单”弹窗：
   - 预期：弹窗宽度在屏幕内完整显示，无横向溢出。
5. 检查表单与评论区：
   - 预期：标签与内容对齐正常，评论区域不挤压、不出现横向滚动。

### 12.3 参数说明（本次改动相关）
| 参数/样式 | 类型 | 说明 |
|---|---|---|
| `.detail-layout` | CSS class | 在 `<=768px` 时切换为 `flex-direction: column` |
| `.detail-sidebar` | CSS class | 手机端取消 sticky，改为常规块级区域并置于主内容下方 |
| `.action-bar-left/.action-bar-right` | CSS class | 手机端占满整行，按钮可自动换行 |
| `.info-form .el-form-item__label` | CSS rule | 手机端标签宽度压缩为 `88px`，减少内容挤压 |
| `el-dialog width` | 组件属性 | 从固定 px 改为 `min(xxxpx, 92vw)`，保证小屏可见 |

### 12.4 返回值说明（交互行为）
| 操作 | 返回值 | 说明 |
|---|---|---|
| 打开工单详情页 | `void` | 页面按屏幕宽度自动应用响应式布局 |
| 点击顶部操作按钮 | `void` | 行为不变，仅布局和点击体验优化 |
| 打开各类弹窗 | `void` | 弹窗逻辑不变，仅宽度自适应 |

### 12.5 常见问题（新增）
#### Q6：为什么我手机上还是老布局（右侧面板很窄、文字挤成一列）？
- **检测**：确认当前页面是否加载了最新前端静态资源。
- **记录（错误类型）**：缓存导致旧 CSS 未更新。
- **恢复建议**：
  1. 强制刷新（`Ctrl+F5` / 清理移动端缓存）；
  2. 重新启动前端服务；
  3. 确认版本已升级到 `v1.1.3-ticket-detail-iphone14pm-layout` 及以上。

#### Q7：本地构建提示 `vue-tsc: not found` 怎么办？
- **检测**：执行 `npm run build` 时提示命令不存在。
- **记录（错误类型）**：本地依赖未安装完整。
- **恢复建议**：
  1. `cd /workspace/miduo-frontend`
  2. `npm install`
  3. 再执行 `npm run build`

### 12.6 版本历史（新增）
| 版本 | 变更内容 |
|---|---|
| `v1.1.3-ticket-detail-iphone14pm-layout` | 工单详情页移动端适配：双栏改单栏、操作区换行、弹窗宽度自适应、表单与评论区间距优化 |



## 13. 工单详情页信息布局 + 附件查看优化（前端）

### 13.1 功能用途
- **用途1：优化“详细信息”页签下的四块布局（客服信息/测试信息/开发信息/时间追踪）**  
  类比：以前像“同一张课桌上摊了四本书，手机上一挤就乱”；现在会按屏幕大小自动调整，手机上更像“一本一本翻着看”。
- **用途2：优化附件图片信息排版**  
  把“文件名、来源、大小、上传人、时间、操作”分层展示，避免文字竖着挤在一起。
- **用途3：点击‘查看’直接预览图片**  
  不再依赖新窗口打开，改成页内弹窗预览，操作更顺手。
- **用途4：角色英文改中文显示**  
  把 `CUSTOMER_SERVICE / TESTER / DEVELOPER` 等编码统一转成中文，阅读更直观。

### 13.2 使用方法（验收步骤）
1. 打开工单详情页，进入“详细信息”页签。
2. 依次点击“客服信息 / 测试信息 / 开发信息 / 时间追踪”。
3. 在手机宽度（<=768）下检查：
   - 表单标签应显示在输入框上方；
   - 内层页签可横向滑动，不会挤压成难读状态。
4. 滚动到“附件”区域：
   - 文件名应可换行显示；
   - 上传人和上传时间应水平展示，不再竖排拥挤。
5. 点击某张图片的“查看”：
   - 预期弹出“图片预览”弹窗，显示大图。
6. 在“时间追踪”和“流转历史”中检查角色字段：
   - 预期显示中文（如“客服/测试/开发”），不再显示英文编码。

### 13.3 参数说明（本次改动相关）
| 参数/方法 | 类型 | 说明 |
|---|---|---|
| `MOBILE_BREAKPOINT` | `number` | 紧凑布局阈值，当前为 `768` |
| `isCompactLayout` | `Computed<boolean>` | 控制内层页签拉伸与表单标签位置 |
| `attachmentPreviewVisible` | `Ref<boolean>` | 控制附件预览弹窗显示 |
| `attachmentPreviewUrl` | `Ref<string>` | 当前预览图片地址 |
| `openAttachmentPreview(filePath)` | `function` | 点击“查看”后打开预览弹窗 |
| `formatRoleLabel(role)` | `function` | 角色编码转中文，支持多角色组合 |
| `roleLabelFn` | `prop` | `TicketTimeTrackPanel` 角色中文渲染函数 |

### 13.4 返回值说明（交互行为）
| 操作 | 返回值 | 说明 |
|---|---|---|
| 点击附件“查看” | `void` | 打开页内图片预览弹窗 |
| 切换详情内层页签 | `void` | 页面按屏幕宽度自动调整标签与表单布局 |
| 查看流转历史/时间追踪角色 | `string` | 返回中文角色标签（由 `formatRoleLabel` 生成） |

### 13.5 常见问题（新增）
#### Q8：为什么我点“查看”还是打开新标签页，不是弹窗？
- **检测**：查看当前页面是否仍存在旧的 `href + target=_blank` 行为。
- **记录（错误类型）**：前端静态资源缓存导致旧模板未更新。
- **恢复建议**：
  1. 强制刷新浏览器（`Ctrl+F5`）；
  2. 清理缓存后重新打开工单详情；
  3. 确认版本已升级到 `v1.1.4-ticket-detail-info-attachment-role-polish` 及以上。

#### Q9：为什么有些角色还是显示英文？
- **检测**：查看该角色编码是否在映射表中（例如新加的角色码）。
- **记录（错误类型）**：角色映射表缺少新编码。
- **恢复建议**：
  1. 在 `miduo-frontend/src/utils/formatter.ts` 的 `ROLE_LABEL_MAP` 中补充映射；
  2. 重新构建并刷新页面验证。

### 13.6 示例截图（终端运行效果）
```text
> miduo-frontend@0.0.0 build
> vue-tsc -b && vite build

vite v7.3.1 building client environment for production...
✓ 1700+ modules transformed.
✓ built in xxs
```

### 13.7 版本历史（新增）
| 版本 | 变更内容 |
|---|---|
| `v1.1.4-ticket-detail-info-attachment-role-polish` | 工单详情页优化：详细信息四块布局适配、附件信息排版优化、图片“查看”弹窗预览、角色英文统一转中文 |
| `v1.1.6-wecom-open-ticket-link-fix` | 修复企业微信工单详情链接404：统一公开链接组装规则、兼容历史错误链接自动跳转、修正测试/生产环境 trusted-domain 默认值 |

---

## 14. 工单看板流转报错中文化（后端）

### 14.1 功能用途
- **用途**：把看板拖拽流转失败时的状态码提示，改成用户能看懂的中文状态名。
- **类比理解**：以前报错像“快递单号”直接给用户看（`pending_accept`）；现在改成“快递状态中文”（`待受理`），一眼就懂。

### 14.2 使用方法（验收步骤）
1. 打开“工单看板”页面，拖拽任意工单到一个你当前无权限或不允许的目标列。
2. 观察报错弹窗（右上角红色提示）。
3. 预期结果：提示中不再出现 `pending_xxx` / `processing` 这类状态码，而是显示“待受理/待分派/处理中”等中文。

### 14.3 参数说明（本次改动相关）
| 参数/方法 | 类型 | 说明 |
|---|---|---|
| `resolveStatusLabel(statusCode)` | `String -> String` | 将工单状态码转换为中文状态名；未知状态则原样返回 |
| `currentStatus` | `String` | 当前状态码（报错组装时转换为中文） |
| `targetStatus` | `String` | 目标状态码（报错组装时转换为中文） |

### 14.4 返回值说明（接口行为）
| 场景 | 返回值 | 说明 |
|---|---|---|
| 非法流转 | `BusinessException` | 报错文案从“从[pending_accept]到[processing]...”变为“从[待受理]到[处理中]...” |
| 不支持退回 | `BusinessException` | 报错文案从“当前状态[pending_xxx]...”变为“当前状态[中文状态]...” |

### 14.5 常见问题（新增）
#### Q10：为什么我页面还是看到 `pending_accept` 这样的英文状态码？
- **检测**：确认后端是否已部署最新代码。
- **记录（错误类型）**：后端服务版本未更新或实例未重启。
- **恢复建议**：
  1. 升级到版本 `v1.1.5-kanban-error-message-zh` 及以上；
  2. 重启后端服务；
  3. 清理浏览器缓存后重试拖拽流转。

---

## 15. 企业微信工单详情链接 404 修复（后端）

### 15.1 功能用途
- **用途**：修复企业微信消息里的“查看详情”链接点击后出现 `Whitelabel Error Page 404` 的问题。
- **类比理解**：原来快递单上的地址写成了“省市区+机房门牌号”，导航会找不到；现在系统会自动把地址“纠偏”成可达的标准地址。

### 15.2 根因（用抓包结果说明）
- 抓包请求是：`GET /api/wecom/callback/open/ticket/{ticketNo}`，服务里没有这个路由，所以 404。
- 同时，`wecom.trusted-domain` 在测试/生产默认值被配置成了 `http://xxx/api/wecom/callback`，代码又拼了 `/open/ticket/{ticketNo}`，导致发出去的链接天然容易错。

### 15.3 本次修复内容
1. **新增统一链接构建器**：`WecomPublicLinkBuilder`
   - 自动去掉错误后缀 `/api/wecom/callback`
   - 再拼接正确路径 `/open/ticket/{ticketNo}`
2. **替换 3 处企微回包逻辑**（创建成功、重复工单、交互确认）统一走构建器
3. **新增历史链接兼容接口**：`GET /api/wecom/callback/open/ticket/{ticketNo}`
   - 接口编号：`API000439`
   - 行为：302 跳转到 `/open/ticket/{ticketNo}`
4. **修正配置默认值**
   - `application-test.yml`：`wecom.trusted-domain` 改为 `http://ticket.t.miduonet.com`
   - `application-prod.yml`：`wecom.trusted-domain` 改为 `http://ticket.ebcone.cn`

### 15.4 使用方法（验收步骤）
1. 在企业微信群里重新创建一条工单，让机器人返回新“查看详情”链接。
2. 点击链接，预期可正常打开工单公开页，不再 404。
3. 用历史消息里的旧链接（包含 `/api/wecom/callback/open/ticket/`）再点一次。
4. 预期会自动跳转到 `/open/ticket/{ticketNo}` 并正常展示。

### 15.5 参数说明（本次修复相关）
| 参数/路径 | 类型 | 说明 |
|---|---|---|
| `wecom.trusted-domain` | `String` | 企微可信域名，应该是域名根地址，不应包含 `/api/wecom/callback` |
| `/open/ticket/{ticketNo}` | `GET` | 工单公开详情真实访问路径 |
| `/api/wecom/callback/open/ticket/{ticketNo}` | `GET` | 历史错误链接兼容入口，302 跳转 |

### 15.6 返回值说明（接口行为）
| 接口 | 返回值 | 说明 |
|---|---|---|
| `GET /api/open/ticket/{ticketNo}` | `ApiResult<TicketPublicDetailOutput>` | 返回公开工单详情数据 |
| `GET /api/wecom/callback/open/ticket/{ticketNo}` | `302 Found` | `Location: /open/ticket/{ticketNo}`，用于老链接兼容 |

### 15.7 常见问题（新增）
#### Q11：修复后为什么旧链接仍然 404？
- **检测**：确认后端是否部署了包含兼容接口的版本。
- **记录（错误类型）**：服务版本未更新或流量仍在旧实例。
- **恢复建议**：
  1. 升级到 `v1.1.6-wecom-open-ticket-link-fix` 及以上；
  2. 重启后端服务并确认路由生效；
  3. 再次使用旧链接测试，观察是否返回 302 跳转。

### 15.8 公开详情页跳转系统内处理（前端）
- **用途**：在企业微信里打开的「无需登录工单详情」页（`/open/ticket/{工单号}`）顶部增加「去系统处理」按钮，新标签页打开与当前站点同域的工单处理页（`/ticket/detail/{数据库id}`）。
- **类比**：公开页像贴在门口的「告示」，按钮是「进办公室办手续」——同一栋楼（域名），进去要登录。
- **说明**：链接里的数字 ID 来自公开详情接口已返回的 `id` 字段；若前后端域名不一致，需自行调整部署或反向代理使两处同源。

#### Q12：为什么 `/open/ticket/{工单号}` 打开是一片白？
- **检测**：按 F12 打开开发者工具 →「网络」里看 `GET /api/open/ticket/{工单号}` 是否 401；看「控制台」是否有 JS 报错或静态资源 404。
- **记录（常见原因）**：
  1. **401 被全局拦截**：公开页请求若返回 401，旧逻辑会强制跳转登录；若 SSO 外链异常，可能表现为白屏。前端已改为在 `/open/` 路径下不因 401 跳转登录，页面会显示接口错误提示。
  2. **网关/安全未放行**：`/api/open/ticket/**` 必须在网关与安全配置中允许匿名访问，否则接口 401/403 工单内容加载失败。
  3. **未返回 SPA**：若 Nginx 未对前端路由做 `try_files ... /index.html`，直接访问 `/open/ticket/...` 可能拿到空响应或非 HTML，页面无法挂载。
- **恢复建议**：确认后端 `SecurityConfig` 已放行 `/api/open/ticket/**`；确认前端静态资源与 `index.html` 可加载；接口仍失败时根据页面上的错误文案或网络面板状态码排查。

---

## 16. 四个页面标题布局优化（前端）

### 16.1 功能用途
- **用途**：统一优化以下 4 个页面顶部标题区域，避免标题被按钮区域“挤成两行”：
  - Bug简报列表
  - Bug简报统计看板
  - 通知中心
  - 组织账号管理
- **类比理解**：像把“黑板标题”和“工具按钮”分好座位，标题固定清晰展示，按钮自己按空间换行，不再抢位置。

### 16.2 使用方法（验收步骤）
1. 进入上述 4 个页面，观察页面顶部标题。
2. 把浏览器宽度从大屏逐步缩小到中等宽度（例如 1200 -> 1024 -> 900）。
3. 预期结果：
   - 标题保持单行显示，不出现被挤压换行；
   - 右侧按钮区域保持可操作，必要时自然换行；
   - 标题与按钮之间间距一致，视觉更整齐。

### 16.3 参数说明（本次改动相关）
| 参数/样式 | 类型 | 说明 |
|---|---|---|
| `.toolbar` / `.card-header` | CSS class | 增加 `flex-wrap: wrap`，避免右侧操作区强行挤压左侧标题 |
| `.title` | CSS class | 增加 `line-height: 24px`、`white-space: nowrap`、`flex-shrink: 0`，保证标题稳定展示 |
| `.toolbar-actions` / `.card-header-right` | CSS class | 设置为 `width: auto; flex: 0 0 auto;`，避免继承全局 `el-space { width: 100% }` 导致标题被压缩 |

### 16.4 返回值说明（交互行为）
| 操作 | 返回值 | 说明 |
|---|---|---|
| 打开页面 | `void` | 页面按新样式渲染，标题区域布局更稳定 |
| 缩放窗口宽度 | `void` | 标题保持单行，操作按钮自适应换行 |

### 16.5 常见问题（新增）
#### Q12：为什么我还是看到标题换行/竖排？
- **检测**：确认浏览器是否加载到最新 CSS（尤其是旧静态资源缓存）。
- **记录（错误类型）**：前端缓存导致样式未更新。
- **恢复建议**：
  1. 强制刷新页面（`Ctrl+F5`）；
  2. 清理浏览器缓存后重试；
  3. 确认版本已升级到 `v1.1.7-page-title-layout-polish` 及以上。

### 16.6 版本历史（新增）
| 版本 | 变更内容 |
|---|---|
| `v1.1.7-page-title-layout-polish` | 统一优化 Bug 简报列表、Bug 简报统计看板、通知中心、组织账号管理四个页面的标题布局，修复标题被右侧按钮挤压换行问题 |

---

## 17. 工作流管理详情：SLA动作与角色可见条件中文化（前端）

### 17.1 功能用途
- **用途**：把“工作流详情”里原本的英文枚举值，改成用户能直接看懂的中文。
- **类比理解**：以前页面像只显示“系统内部代号”，例如 `START_RESPONSE`、`TICKET_ADMIN`；现在会显示“开始响应计时”“工单管理员”，就像把快递状态码翻译成“待揽收/运输中”。

### 17.2 使用方法（验收步骤）
1. 进入「管理 -> 工作流管理」页面。
2. 点击任意工作流的「详情」。
3. 在「状态图配置」区域检查“**SLA动作**”列：
   - 预期：显示中文（如“开始响应计时/开始解决计时/暂停计时/停止计时”）。
4. 在「流转规则与角色可见条件」区域检查“**角色可见条件**”列：
   - 预期：显示中文角色（如“系统管理员、工单管理员、测试、处理人”），不再直接显示英文枚举。

### 17.3 参数说明（本次改动相关）
| 参数/方法 | 类型 | 说明 |
|---|---|---|
| `SLA_ACTION_LABEL_MAP` | `Record<string, string>` | SLA动作英文枚举到中文名称的映射表 |
| `WORKFLOW_ROLE_LABEL_MAP` | `Record<string, string>` | 角色英文枚举到中文名称的映射表 |
| `getSlaActionLabel(action)` | `function` | 将 SLA 动作转换为中文，空值显示 `-` |
| `formatAllowedRoles(roles)` | `function` | 将角色数组转换为中文串，使用 `、` 连接 |

### 17.4 返回值说明（页面行为）
| 操作 | 返回值 | 说明 |
|---|---|---|
| 打开工作流详情 | `void` | 展示详情抽屉，SLA动作与角色可见条件按中文渲染 |
| 查看状态配置/流转规则 | `string` | 未识别枚举保持原值；空值显示 `-`，避免空白 |

### 17.5 常见问题（新增）
#### Q13：为什么我还是看到英文枚举？
- **检测**：确认当前前端是否已加载最新静态资源（尤其是浏览器缓存）。
- **记录（错误类型）**：前端缓存导致页面仍使用旧 JS。
- **恢复建议**：
  1. 强制刷新页面（`Ctrl+F5`）；
  2. 重新启动前端服务并再次访问；
  3. 确认版本已升级到 `v1.1.8-workflow-detail-zh-labels` 及以上。

### 17.6 版本历史（新增）
| 版本 | 变更内容 |
|---|---|
| `v1.1.8-workflow-detail-zh-labels` | 工作流详情页优化：SLA动作、角色可见条件由英文枚举改为中文显示，提升管理端可读性 |

---

## 18. 工作流详情页状态名称中文化补齐（前端）

### 18.1 功能用途
- **用途**：把“工作流详情 -> 流转规则”里的英文状态码（如 `investigating`、`processing`）改成中文状态名显示。
- **类比理解**：之前像菜单上只写“菜品编号”，需要人自己猜；现在直接写“菜名”，管理同事一眼就能看懂。

### 18.2 使用方法（验收步骤）
1. 进入「管理 -> 工作流管理」页面。
2. 点击任意工作流的「详情」按钮。
3. 在「流转规则与角色可见条件」表格中检查“起始状态/目标状态”两列。
4. 预期结果：
   - `investigating` 显示为“排查中”
   - `processing` 显示为“处理中”
   - `temp_resolved` 显示为“临时解决”
   - `suspended` 显示为“已挂起”
   - 其他已支持状态同样显示中文。

### 18.3 参数说明（本次改动相关）
| 参数/方法 | 类型 | 说明 |
|---|---|---|
| `WORKFLOW_STATE_LABEL_MAP` | `Record<string, string>` | 工作流状态码到中文状态名的映射表 |
| `getWorkflowStateLabel(state)` | `function` | 将状态值统一转换成中文；空值返回 `-`；未知状态保留原值 |

### 18.4 返回值说明（页面行为）
| 操作 | 返回值 | 说明 |
|---|---|---|
| 打开工作流详情 | `void` | 页面展示流转规则表格 |
| 渲染起始/目标状态列 | `string` | 返回中文状态名；未识别状态原样返回 |

### 18.5 常见问题（新增）
#### Q14：为什么状态列还是英文？
- **检测**：确认浏览器是否加载了最新前端静态资源。
- **记录（错误类型）**：前端缓存导致旧脚本未更新。
- **恢复建议**：
  1. 强制刷新页面（`Ctrl+F5`）；
  2. 重新启动前端服务；
  3. 确认版本已升级到 `v1.1.9-workflow-transition-state-zh` 及以上。

### 18.6 版本历史（新增）
| 版本 | 变更内容 |
|---|---|
| `v1.1.9-workflow-transition-state-zh` | 工作流详情页补齐状态中文化：将流转规则中的起始状态、目标状态由英文状态码改为中文显示 |

---

## 19. 全站分页组件英文改中文（前端）

### 19.1 功能用途
- **用途**：把系统里所有分页组件的英文文案统一改成中文，例如：
  - `Total 78` → `共 78 条`
  - `20/page` → `20条/页`
  - `Go to` → `前往`
- **类比理解**：相当于把全站“路牌语言”统一成中文，不管你走到哪个页面，分页文案都一致、好理解。

### 19.2 使用方法（验收步骤）
1. 打开任意含分页的页面（如工单列表、通知中心、用户管理、工作流管理）。
2. 查看分页条中的总数、每页条数、跳转输入区域。
3. 预期结果：
   - 总数显示“共 X 条”
   - 每页条数显示“X条/页”
   - 跳转前缀显示“前往”
4. 横向检查多个页面，预期文案一致，不再出现英文。

### 19.3 参数说明（本次改动相关）
| 参数/配置 | 类型 | 说明 |
|---|---|---|
| `ElementPlus locale` | `object` | 在应用入口挂载中文语言包，统一控制组件文案 |
| `zhCn` | `Locale` | Element Plus 官方中文包（`element-plus/es/locale/lang/zh-cn`） |

### 19.4 返回值说明（页面行为）
| 操作 | 返回值 | 说明 |
|---|---|---|
| 打开任意分页页面 | `void` | 分页控件自动按中文语言包渲染 |
| 切换每页条数/页码 | `void` | 交互逻辑不变，仅展示文案中文化 |

### 19.5 常见问题（新增）
#### Q15：为什么我有些页面分页还是英文？
- **检测**：确认浏览器是否仍在使用旧缓存资源。
- **记录（错误类型）**：前端缓存导致入口语言配置未生效。
- **恢复建议**：
  1. 强制刷新（`Ctrl+F5`）；
  2. 重新启动前端服务；
  3. 确认版本已升级到 `v1.2.0-pagination-zh-locale` 及以上。

### 19.6 版本历史（新增）
| 版本 | 变更内容 |
|---|---|
| `v1.2.0-pagination-zh-locale` | 在前端入口启用 Element Plus 中文语言包，统一全站分页组件英文文案为中文显示 |

---

## 20. 全局下拉框交互“轻盈化”优化（前端）

### 20.1 功能用途
- **用途**：把全站 `el-select` 的点击展开体验做统一优化，减少“点开像卡一下”的体感。
- **类比理解**：以前像“抽屉有点发涩，拉开要顿一下”；现在像“滑轨上了润滑油”，开合更顺手。

### 20.2 使用方法（验收步骤）
1. 打开任意带下拉框页面（如：所有工单、通知中心、用户管理、工作流管理）。
2. 连续点击同一个下拉框 5~10 次，观察展开/收起速度和连贯性。
3. 快速移动鼠标经过选项列表，观察 hover 高亮是否跟手。
4. 预期结果：
   - 打开与关闭更干脆（无明显“拖影/顿挫”）；
   - 选项 hover 与选中反馈更清晰；
   - 全局页面效果一致。

### 20.3 参数说明（本次改动相关）
| 参数/样式 | 类型 | 说明 |
|---|---|---|
| `--md-dropdown-motion-duration` | CSS 变量 | 下拉框开合和边框反馈时长，当前 `0.14s` |
| `--md-dropdown-motion-ease` | CSS 变量 | 下拉动效曲线，当前 `cubic-bezier(0.22, 1, 0.36, 1)` |
| `.el-select .el-select__wrapper` | CSS 规则 | 优化输入框容器过渡效果，减少“硬切” |
| `.el-select__popper.el-popper` | CSS 规则 | 优化弹层阴影、圆角和合成层，减少抖动感 |
| `.el-zoom-in-top-*` | CSS 规则 | 统一下拉弹层进入/离开动画速度与位移 |
| `@media (prefers-reduced-motion: reduce)` | CSS 规则 | 系统偏好减少动效时自动降动画，保证可访问性 |

### 20.4 返回值说明（交互行为）
| 操作 | 返回值 | 说明 |
|---|---|---|
| 点击下拉框 | `void` | 仅优化交互动画与视觉反馈，不改变业务逻辑 |
| 选择某个选项 | `void` | 仅优化 hover/selected 反馈，不改变数据提交 |

### 20.5 常见问题（新增）
#### Q16：为什么我感觉还是旧的“卡顿感”？
- **检测**：确认浏览器是否使用了旧 CSS 缓存。
- **记录（错误类型）**：静态资源缓存导致样式未更新。
- **恢复建议**：
  1. 强制刷新（`Ctrl+F5`）；
  2. 重启前端服务后再次验证；
  3. 确认版本已升级到 `v1.2.1-global-select-smoothness` 及以上。

### 20.6 版本历史（新增）
| 版本 | 变更内容 |
|---|---|
| `v1.2.1-global-select-smoothness` | 全局优化 Element Plus 下拉框交互：开合动效缩短、弹层过渡更平滑、hover/选中反馈增强、支持 reduced-motion |

---

## 21. 日报推送“凌晨触发 + 重复两条”修复（后端）

### 21.1 功能用途
- **用途**：修复“配置是每天 09:00 + 工作日 18:00，但凌晨 02:00 仍推送，且一次发两条”的问题。
- **类比理解**：以前像“闹钟按了两个规则、还放在错误时区，两个手机一起响”；现在改成“同一个时区校准 + 同一分钟只允许响一次”。

### 21.2 使用方法（验收步骤）
1. 在“日报管理”里配置推送时间为：
   - `每天 09:00`（`0 0 9 * * ?`）
   - `工作日 18:00`（`0 0 18 * * MON-FRI`）
2. 确认系统基础设置中的时区为业务时区（日本可设为 `Asia/Tokyo`）。
3. 到触发时刻观察企微群消息：
   - 预期每个触发时刻仅收到 1 条；
   - 不会在凌晨 02:00 出现“错时推送”。
4. 若有多实例部署（多台后端），同一时刻也应只收到 1 条。

### 21.3 参数说明（本次改动相关）
| 参数/常量 | 类型 | 说明 |
|---|---|---|
| `DailyReportApplicationService.getScheduleTimezone()` | `String` | 读取 `system_config(BASIC/timezone)` 作为日报 cron 计算时区，非法值回退 `Asia/Shanghai` |
| `daily_report_cron` | `String` | 多 cron 配置（`;` 分隔），解析时自动去重，避免同一 cron 重复保存后重复触发 |
| `RedisKeyConstants.DAILY_REPORT_PUSH_DEDUP_PREFIX` | `String` | 分钟级去重 key 前缀，确保同一分钟只推一次（跨实例生效） |

### 21.4 返回值说明（接口行为）
| 接口 | 返回值 | 说明 |
|---|---|---|
| `PUT /api/daily-report/config` | `ApiResult<Void>` | 保存配置成功后，定时任务按新时区和新 cron 重新装载 |
| `POST /api/daily-report/push` | `ApiResult<Void>` | 手动推送仍可正常发送，不受自动任务分钟去重影响 |

### 21.5 常见问题（新增）
#### Q17：为什么会在凌晨 02:00 推送？
- **检测**：检查基础配置里的 `timezone` 是否与业务期望时区一致（如日本应为 `Asia/Tokyo`）。
- **记录（错误类型）**：定时任务按服务器默认时区/错误时区计算 cron，触发时间发生偏移。
- **恢复建议**：
  1. 在系统基础设置中把时区改为正确值；
  2. 保存日报配置后观察日志中的 `timezone` 与 `nextExecuteTime`；
  3. 确认触发时刻与页面设置一致。

#### Q18：为什么同一时刻会收到两条日报？
- **检测**：检查是否配置了重叠 cron（如同一个时刻两条规则）或是否是多实例部署。
- **记录（错误类型）**：同轮多 cron 命中重复触发，或多实例并发触发导致重复推送。
- **恢复建议**：
  1. 升级到版本 `v1.2.2-daily-report-schedule-dedup-timezone-fix` 及以上；
  2. 保留所需 cron，避免逻辑重复配置；
  3. 验证日志中“同一分钟已推送，跳过本轮”去重提示是否生效。

### 21.6 版本历史（新增）
| 版本 | 变更内容 |
|---|---|
| `v1.2.2-daily-report-schedule-dedup-timezone-fix` | 修复日报自动推送错时与重复：按系统时区计算 cron、同轮多 cron 命中只推一次、分钟级 Redis 去重防止多实例重复推送 |

---

## 22. Bug简报手机端体验优化（前端）

### 22.1 功能用途
- **用途1：压缩列表页“操作栏”宽度**  
  以前像“把 4 个大按钮并排塞进手机一行”，会把表格挤得很难看；现在手机端改成“一个【操作】入口 + 下拉菜单”，不再占用过宽空间。
- **用途2：Bug简报详情页改为手机友好布局**  
  以前详情页是桌面思路（大表格+多列），手机上会显得挤、难点、难读；现在改成“信息分块卡片 + 单列展示 + 大触控区”，浏览和操作更顺手。

### 22.2 使用方法（验收步骤）
1. 打开 `Bug简报列表` 页面，在手机宽度（<=768）查看“操作”列。
2. 预期：每行只显示“操作”按钮，点击后弹出“详情/编辑/提交审核/作废”菜单（按状态显示）。
3. 打开任意 `Bug简报详情` 页面，在手机宽度下检查：
   - 顶部操作区显示“返回列表 / 一键复制 / 更多操作”；
   - 明细信息按单列卡片显示，不出现内容被截断或布局错乱；
   - 关联工单、附件、状态日志改为卡片列表，便于手指点击；
   - 提交审核、审核通过/驳回弹窗宽度可自适应手机屏幕。

### 22.3 参数说明（本次改动相关）
| 参数/状态 | 类型 | 说明 |
|---|---|---|
| `MOBILE_BREAKPOINT` | `number` | 手机断点，当前为 `768` |
| `isMobile`（列表页） | `Ref<boolean>` | 控制操作列在手机端切换为下拉菜单 |
| `handleMobileOperationCommand` | `function` | 处理列表页手机端操作菜单命令 |
| `isMobile`（详情页） | `Ref<boolean>` | 控制详情页在手机端切换为单列/卡片布局 |
| `handleMobileActionCommand` | `function` | 处理详情页“更多操作”菜单命令 |
| `mobile-block-*` 样式 | CSS class | 关联工单/附件/日志的手机卡片化展示 |

### 22.4 返回值说明（交互行为）
| 操作 | 返回值 | 说明 |
|---|---|---|
| 列表页点击“操作” | `void` | 打开下拉菜单并触发对应动作 |
| 详情页点击“更多操作” | `void` | 按命令执行编辑/提交审核/审核/作废 |
| 打开提交审核/审核弹窗 | `void` | 弹窗宽度自动适配手机（`92vw`） |

### 22.5 常见问题（新增）
#### Q19：为什么手机上“操作栏”还是很宽？
- **检测**：确认当前是否仍加载旧静态资源。
- **记录（错误类型）**：前端缓存导致旧模板未更新。
- **恢复建议**：
  1. 强制刷新（`Ctrl+F5`）；
  2. 重新打开 Bug 简报列表；
  3. 确认版本升级到 `v1.2.3-bugreport-mobile-ux-polish` 及以上。

#### Q20：为什么详情页在手机上看起来还是表格样式？
- **检测**：确认浏览器宽度是否已进入手机断点（<=768）。
- **记录（错误类型）**：断点未命中或缓存未更新。
- **恢复建议**：
  1. 切换为设备模式（如 iPhone 12/14）；
  2. 强制刷新页面；
  3. 若仍异常，检查控制台是否有前端报错阻断渲染。

### 22.6 版本历史（新增）
| 版本 | 变更内容 |
|---|---|
| `v1.2.3-bugreport-mobile-ux-polish` | 优化 Bug 简报手机端体验：列表操作列改下拉菜单，详情页改单列卡片化布局，弹窗宽度自适应，操作触控更友好 |

---

## 23. 工单详情「时间追踪」空白区域视觉优化（前端）

### 23.1 功能用途
- **用途**：修复工单详情页「详细信息 -> 时间追踪」在手机端出现“右侧大块空白”的问题，让内容更饱满、更紧凑。
- **类比理解**：以前像“把一张小卡片放在大白纸左边”，右边浪费很多空位；现在改成“卡片自动撑满可用宽度”，像手机资讯流一样自然铺开。

### 23.2 使用方法（验收步骤）
1. 打开任意工单详情，进入「详细信息 -> 时间追踪」。
2. 切换到手机设备模式（建议 iPhone 12/14，宽度 390/430）。
3. 观察「时间链」中每条记录卡片：
   - 预期：卡片宽度铺满，不再出现右侧明显空白条。
4. 观察「本环节填写 / 变更」字段区：
   - 预期：字段名与内容在小屏自动换为上下布局，文本和图片不挤压。
5. 观察「节点耗时统计」表格：
   - 预期：可横向滑动查看全部列，不会把主页面挤坏。

### 23.3 参数说明（本次改动相关）
| 参数/样式 | 类型 | 说明 |
|---|---|---|
| `.ticket-time-track-panel` | CSS class | 作为时间追踪主容器，增加 `overflow: hidden`，避免子元素溢出造成假空白 |
| `.track-item` | CSS class | 记录卡片改为 `width: 100%`，确保内容占满当前区域 |
| `.track-item :deep(.field-change-block)` | CSS rule | 强制字段变更块在卡片内 100% 展开 |
| `.field-row`（<=768） | CSS rule | 移动端改为纵向排列，减少横向挤压与留白 |
| `.field-label`（<=768） | CSS rule | 标签宽度从固定值改为自适应，防止内容区被压窄 |
| `.track-block :deep(.el-table__inner-wrapper)` | CSS rule | 节点耗时统计表格容器宽度锁定为 100% |

### 23.4 返回值说明（交互行为）
| 操作 | 返回值 | 说明 |
|---|---|---|
| 进入时间追踪页签 | `void` | 按屏幕宽度自动应用紧凑布局 |
| 查看字段变更内容 | `void` | 手机端自动换行为纵向结构，提升可读性 |
| 查看节点耗时统计 | `void` | 交互逻辑不变，仅优化展示宽度和可读性 |

### 23.5 常见问题（新增）
#### Q21：为什么我手机上还是看到右侧大片空白？
- **检测**：检查是否仍在使用浏览器缓存的旧样式文件。
- **记录（错误类型）**：前端静态资源缓存导致 CSS 未生效。
- **恢复建议**：
  1. 强制刷新（`Ctrl+F5`）；
  2. 清理移动端缓存后重新打开工单详情；
  3. 确认版本升级到 `v1.2.4-ticket-time-track-mobile-layout-polish` 及以上。

### 23.6 版本历史（新增）
| 版本 | 变更内容 |
|---|---|
| `v1.2.4-ticket-time-track-mobile-layout-polish` | 优化工单详情“时间追踪”在手机端的展示：修复右侧留白、字段区纵向排布、记录卡片铺满容器、节点耗时表格容器宽度优化 |

---

## 24. 工单详情时间追踪：防止被“节点耗时统计”撑开（前端）

### 24.1 功能用途
- **用途**：修复工单详情里「时间追踪」区域被「节点耗时统计」表格撑开的布局问题，避免上方时间链内容被挤压。
- **类比理解**：以前像“把超长表格直接塞进抽屉，抽屉被顶到变形”；现在改成“抽屉本身有滑轨”，表格在自己的区域里滚动，不去挤上面的内容。

### 24.2 使用方法（验收步骤）
1. 打开任意工单详情，进入「详细信息 -> 时间追踪」。
2. 准备一条节点较多、字段较多的工单（让节点耗时统计列较宽）。
3. 在桌面端检查：
   - 预期：节点耗时统计出现在独立滚动区，表格过宽时可左右滚动；
   - 预期：时间链区域不被表格“顶开”导致异常拉伸。
4. 在手机端（<=768）检查：
   - 预期：节点耗时统计自动走卡片模式（不再强制渲染大表格）；
   - 预期：时间追踪整体阅读顺序自然，不出现大块被挤压错位。

### 24.3 参数说明（本次改动相关）
| 参数/样式 | 类型 | 说明 |
|---|---|---|
| `:compact="isCompactLayout"` | Vue prop | 将详情页移动端判断传给 `TicketTimeTrackPanel`，手机端使用卡片模式 |
| `.node-duration-table-wrap` | CSS class | 节点耗时统计表格外层滚动容器，负责横向滚动与边框包裹 |
| `el-table max-height="420"` | 组件属性 | 限制表格可视高度，超出时表格内部滚动，避免持续撑高页面 |
| `.node-duration-table-wrap .el-table { min-width: 1360px; }` | CSS rule | 维持表格列最小宽度，保证列内容不被强行压扁 |

### 24.4 返回值说明（交互行为）
| 操作 | 返回值 | 说明 |
|---|---|---|
| 打开时间追踪页签 | `void` | 页面自动根据屏幕宽度应用桌面表格/移动端卡片模式 |
| 横向查看节点耗时统计 | `void` | 仅改变展示容器滚动行为，不影响数据与业务逻辑 |

### 24.5 常见问题（新增）
#### Q22：为什么我这里还是被表格撑开？
- **检测**：查看浏览器是否还在用旧前端静态资源（样式未更新）。
- **记录（错误类型）**：前端缓存导致新布局规则未生效。
- **恢复建议**：
  1. 强制刷新（`Ctrl+F5`）；
  2. 重新打开工单详情并切到时间追踪；
  3. 确认版本升级到 `v1.2.5-ticket-time-track-node-duration-layout-fix` 及以上。

#### Q23：构建时报 `vue-tsc: not found` 怎么办？
- **检测**：执行 `npm run build` 时提示找不到 `vue-tsc`。
- **记录（错误类型）**：前端依赖未安装。
- **恢复建议**：
  1. `cd /workspace/miduo-frontend`
  2. `npm install`
  3. 重新执行 `npm run build`

### 24.6 版本历史（新增）
| 版本 | 变更内容 |
|---|---|
| `v1.2.5-ticket-time-track-node-duration-layout-fix` | 修复工单详情“时间追踪”被节点耗时统计表格撑开：移动端正确启用卡片模式，桌面端表格加入独立滚动容器与高度限制 |

---

## 25. 新建工单后企微未@处理人修复（后端）

### 25.1 功能用途
- **用途**：修复“新建工单时系统里有处理人，但企业微信群消息没有真正@到处理人”的问题。  
- **类比理解**：以前像“快递单上写了收件人名字，但没贴门牌号”，快递员（企微）找不到人；现在系统会同时带上“门牌号A（wecom_userid）”和“门牌号B（手机号兜底）”，提高@命中率。

### 25.2 根因说明（对应你反馈的现象）
- 这类消息走的是 `WebhookDispatchService` 的企业微信机器人 `text` 消息。
- 旧逻辑只在少数场景拼 `mentioned_list`（企微userid），如果用户资料里 `wecom_userid` 缺失或未同步，就会出现“消息显示了指派给XXX，但没有真正@到人”。

### 25.3 本次修复内容
1. 把@对象收集逻辑升级为统一的 `collectMentionTargets(...)`：
   - 收集创建人、当前处理人、原处理人、操作人等候选人；
   - 一次性批量查用户，避免重复查库。
2. 发送企微机器人 `text` 消息时：
   - **优先**写入 `mentioned_list`（wecom_userid）；
   - **兜底**写入 `mentioned_mobile_list`（手机号，自动清洗 `+86/空格/横杠`）。
3. 处理人没有 `wecom_userid` 时，不再直接漏@，会自动回退到手机号@。

### 25.4 使用方法（验收步骤）
1. 在“新建工单”页面选择一个处理人后提交工单。
2. 到对应企微群查看“工单事件通知”消息。
3. 预期结果：
   - “变更：指派给：XXX”正常显示；
   - 群里会出现真正的@提醒（处理人应收到被@通知）。
4. 建议额外验证两个用户：
   - A用户：有 `wecom_userid`（应走 `mentioned_list`）；
   - B用户：无 `wecom_userid` 但有手机号（应走 `mentioned_mobile_list` 兜底）。

### 25.5 参数说明（本次修复相关）
| 字段 | 类型 | 说明 |
|---|---|---|
| `text.mentioned_list` | `string[]` | 企微机器人@用户列表（企微 `userid`） |
| `text.mentioned_mobile_list` | `string[]` | 企微机器人@手机号列表（本次新增兜底） |
| `sys_user.wecom_userid` | `string` | 用户企微唯一标识，优先使用 |
| `sys_user.phone` | `string` | 手机号兜底来源，会自动做格式清洗 |

### 25.6 返回值说明（接口行为）
| 场景 | 返回值 | 说明 |
|---|---|---|
| Webhook 推送成功 | `2xx` | 企微机器人收到标准 `text` 消息体，包含@字段 |
| Webhook 推送失败 | 记录失败日志 | 在 webhook_dispatch_log 中可查看请求体、响应码和失败原因 |

### 25.7 常见问题（新增）
#### Q24：为什么消息里还是没有@？
- **检测**：在管理端查看 webhook 分发日志，确认请求体里是否包含 `mentioned_list` 或 `mentioned_mobile_list`。
- **记录（错误类型）**：用户资料缺少可用的企微userid和手机号，或手机号格式不合法。
- **恢复建议**：
  1. 优先补齐 `sys_user.wecom_userid`；
  2. 若暂未同步企微userid，确保 `sys_user.phone` 为有效手机号；
  3. 重新新建工单触发一次通知并复测。

### 25.8 版本历史（新增）
| 版本 | 变更内容 |
|---|---|
| `v1.2.6-wecom-mention-assignee-fallback-mobile` | 修复新建工单后企微未@处理人：企业微信Webhook消息新增手机号@兜底（mentioned_mobile_list），并统一@目标收集逻辑 |
| `v1.2.7-scroll-inertia-touch-smoothness` | 全站滚动体验优化：PC端新增惯性滚动（接近 Apple 丝滑感），移动端触摸滚动更跟手并降低卡顿体感 |

---

## 26. 全站滚动体验优化（PC 惯性 + 移动端跟手）

### 26.1 功能用途
- **用途1：PC 端滚动更像 Apple 的“顺滑惯性”**  
  以前像“推一个重箱子，停得生硬”；现在像“带阻尼的滑轨”，滚轮滚动会有自然缓冲，不会一顿一顿。
- **用途2：移动端触摸滑动更跟手**  
  以前像“手指在玻璃上滑，页面偶尔拖后腿”；现在通过原生触摸滚动参数优化，让手指和页面更同步。
- **用途3：减少复杂页面卡顿体感**  
  对主滚动区和侧边菜单滚动做轻量优化，优先保证“流畅感”和“可控性”。

### 26.2 使用方法（验收步骤）
1. 打开任意长页面（如“所有工单”“通知中心”），在桌面端快速滚轮上下滚动。
2. 预期：滚动有连续惯性，不会突然“硬停”。
3. 在手机设备模式（如 iPhone 12/14）上下滑动页面和左侧菜单区域。
4. 预期：页面跟手、惯性自然，滑动后回弹与续滑更接近原生 App。
5. 在页面内部可滚动区域（如通知抽屉列表）继续测试。
6. 预期：内部区域还能正常滚动，不会被全局滚动劫持。

### 26.3 参数说明（本次改动相关）
| 参数/方法 | 类型 | 说明 |
|---|---|---|
| `createInertiaWheelScroll(container, options)` | `function` | 创建桌面端滚轮惯性控制器（仅鼠标/触控板环境启用） |
| `lerpFactor` | `number` | 惯性插值系数，越小越“柔和”，当前使用 `0.14` |
| `maxDeltaPerFrame` | `number` | 每帧最大位移，避免单帧跳太远，当前使用 `220` |
| `DESKTOP_POINTER_MEDIA_QUERY` | `string` | 仅在 `(hover: hover) and (pointer: fine)` 环境启用惯性滚动 |
| `.main` `touch-action: pan-y pinch-zoom` | CSS | 允许垂直滑动 + 双指缩放，提升移动端触摸跟手性 |
| `.main` `-webkit-overflow-scrolling: touch` | CSS | 开启 iOS 原生惯性滚动 |
| `.menu` `-webkit-overflow-scrolling: touch` | CSS | 侧边菜单滚动同样采用原生惯性 |

### 26.4 返回值说明（交互行为）
| 操作 | 返回值 | 说明 |
|---|---|---|
| 桌面端滚轮滚动主内容区 | `void` | 应用惯性动画，不改变业务数据 |
| 移动端手指滑动主内容区/菜单区 | `void` | 使用原生触摸滚动机制，提升跟手性 |
| 切换到手机宽度（<=768） | `void` | 自动关闭桌面惯性引擎，避免和触摸冲突 |

### 26.5 常见问题（新增）
#### Q25：为什么我感觉不到新滚动效果？
- **检测**：确认是否在桌面端且使用滚轮/触控板测试；手机端不会走桌面惯性引擎。
- **记录（错误类型）**：测试场景不匹配（移动端触摸不会启用桌面 wheel 惯性）。
- **恢复建议**：
  1. 在桌面浏览器用滚轮连续快速滚动长页面；
  2. 强制刷新页面（`Ctrl+F5`）；
  3. 确认版本已升级到 `v1.2.7-scroll-inertia-touch-smoothness` 及以上。

#### Q26：为什么某个弹窗内部滚动感觉和主页面不一样？
- **检测**：查看该区域是否为独立滚动容器（例如组件内部的滚动条）。
- **记录（错误类型）**：局部滚动容器与主容器采用不同滚动策略（预期行为）。
- **恢复建议**：
  1. 先确认主页面滚动是否已生效；
  2. 如需统一某个具体弹窗，再单独对该容器做同类优化。

---

## 27. 企业微信工单事件通知排版优化（后端）

### 27.1 功能用途
- **用途1：标题和内容分区显示**  
  把原来“一坨文本”改为“标题区 + 信息区 + 变更区 + 详情区”，阅读顺序更清楚。
- **用途2：变更内容拆行**  
  把“分类、优先级、分派类型、操作人”等变更项从逗号拼接改成逐条编号展示，方便快速扫读。
- **用途3：优先级可视化区分**  
  使用颜色圆点符号增强优先级识别：`🔴紧急 / 🟠高 / 🟡中 / 🟢低`。

### 27.2 使用方法（验收步骤）
1. 在系统中触发任意工单事件（如：工单创建、分派、状态变更）。
2. 确保该事件会通过 Webhook 推送到企业微信群机器人。
3. 在企业微信群里查看消息。
4. 预期结果：
   - 顶部显示“【工单事件通知】”和事件信息；
   - 工单基础信息按编号分行展示；
   - 变更内容按序号逐条展示，不再堆在一行；
   - 优先级显示带颜色标识（圆点符号）。

### 27.3 参数说明（本次改动相关）
| 参数/方法 | 类型 | 说明 |
|---|---|---|
| `buildWecomRobotTextPayload(...)` | `String -> String(JSON)` | 企业微信机器人消息体组装入口，本次改为结构化分区排版 |
| `buildChangeLines(data)` | `Object -> List<String>` | 将事件变更数据拆成多行列表，替代旧的单行逗号拼接 |
| `resolvePriorityDisplay(code)` | `String -> String` | 输出带颜色标识的优先级文本（🔴🟠🟡🟢） |
| `TicketStatus#isPendingAcceptanceLike()` | `boolean` | 待分派/待受理/待测试受理/待开发受理等「待受理类」状态，用于决定是否展示当前处理人 |
| `WECOM_TEXT_MAX_BYTES` | `int` | 企业微信文本消息字节上限，超出自动截断 |

**状态变更类通知补充**：
- 当事件为 `TICKET_STATUS_CHANGED` 且工单当前状态为待受理类时，在「工单信息」中增加一行 **当前处理人**（按 `assigneeId` 解析姓名）。
- 「变更内容」各编号行之后增加一行 **变更时间**（与顶部「时间」一致，优先使用负载中的 `eventTime`）。

### 27.4 返回值说明（接口行为）
| 场景 | 返回值 | 说明 |
|---|---|---|
| Webhook 是企业微信机器人地址 | `msgtype=text` JSON | 自动转为企业微信可接收格式，并应用新排版 |
| 事件包含变更数据 | `text.content` 多行文本 | “变更内容”按行输出，便于阅读 |
| 优先级存在 | `text.content` 优先级字段 | 显示“颜色符号 + 中文优先级” |

### 27.5 常见问题（新增）
#### Q27：为什么没有看到“颜色区分”？
- **检测**：检查企业微信群消息中优先级是否显示 `🔴🟠🟡🟢` 前缀。
- **记录（错误类型）**：仍在使用旧版本后端，或消息来自未走该链路的其他通知通道。
- **恢复建议**：
  1. 升级到版本 `v1.2.8-wecom-ticket-event-layout-priority-visual` 及以上；
  2. 重新触发工单事件；
  3. 在 webhook 分发日志中确认命中 `WebhookDispatchService` 企业微信适配分支。

#### Q28：为什么“变更内容”还是一整行？
- **检测**：查看消息来源是否是企业微信机器人 Webhook（`qyapi.weixin.qq.com/cgi-bin/webhook/send`）。
- **记录（错误类型）**：非企业微信机器人通道不会走本次文本组装逻辑。
- **恢复建议**：
  1. 确认目标通知配置使用企业微信机器人地址；
  2. 重新发送后验证消息格式。

### 27.6 版本历史（新增）
| 版本 | 变更内容 |
|---|---|
| `v1.2.8-wecom-ticket-event-layout-priority-visual` | 优化企业微信“工单事件通知”排版：标题/内容分区、变更项拆行、优先级颜色标识（符号）显示，保留原有通知信息不丢失 |

---

## 28. 通知抽屉“刷新”报 BindException 修复（前端）

### 28.1 功能用途
- **用途**：修复点击通知抽屉里的“刷新”按钮后，后端报 `pageSize=[object PointerEvent]` 的错误。
- **类比理解**：原来像把“鼠标点击动作”当成了“每页条数”传给后端；现在会先把参数“过安检”，只让数字通过。

### 28.2 使用方法（验收步骤）
1. 登录系统，点击顶部铃铛图标打开“通知消息”抽屉。
2. 点击抽屉右上角“刷新”按钮。
3. 预期结果：
   - 不再出现“系统内部错误 - BindException ... pageSize=[object PointerEvent]”；
   - 通知列表和未读数可正常刷新。
4. 再进入“通知中心”页面做一次列表查询和翻页，确认通知相关功能都正常。

### 28.3 参数说明（本次修复相关）
| 参数/方法 | 类型 | 说明 |
|---|---|---|
| `handleRefreshNotificationOverview()` | `() => void` | 抽屉“刷新”按钮专用处理函数，确保调用 `refreshOverview` 时不传点击事件对象 |
| `refreshOverview(pageSize)` | `(unknown) => Promise<void>` | 入参扩展为 `unknown`，内部先做数字化校验 |
| `normalizePageSize(raw, fallback)` | `(unknown, number) => number` | 将 `pageSize` 统一收敛为正整数；非法值（如 PointerEvent）回退默认值 |

### 28.4 返回值说明（交互行为）
| 操作 | 返回值 | 说明 |
|---|---|---|
| 点击通知抽屉“刷新” | `void` | 前端发起刷新请求，后端接收到合法 `pageSize` |
| 调用 `refreshOverview`（异常入参） | `Promise<void>` | 自动回退到默认页大小，不再触发类型绑定异常 |

### 28.5 常见问题（新增）
#### Q29：为什么会出现 `[object PointerEvent]` 这种奇怪参数？
- **检测**：查看报错字段是否是 `pageSize`，值是否为 `[object PointerEvent]`。
- **记录（错误类型）**：前端事件对象误传（点击事件被当成业务参数）。
- **恢复建议**：
  1. 不要在模板里直接把“需要业务参数的方法”裸绑到 `@click`；
  2. 通过中间处理函数调用，或在方法内部做参数类型收敛；
  3. 升级到版本 `v1.2.9-notification-refresh-pointerevent-fix` 及以上。

### 28.6 版本历史（新增）
| 版本 | 变更内容 |
|---|---|
| `v1.2.9-notification-refresh-pointerevent-fix` | 修复通知抽屉点击“刷新”触发 `BindException(pageSize=[object PointerEvent])`：按钮改用中间处理函数 + 通知 store 增加 pageSize 参数兜底校验 |

---

## 29. 追加处理人后工单通知“指派给”与@修复（后端）

### 29.1 功能用途
- **用途**：修复“追加处理人后，工单通知里‘指派给’没变、且没有@到新增处理人”的问题。  
- **类比理解**：以前像“班级值日表加了新同学，但群通知还发旧名单，也没@新同学”；现在通知会按最新值日表发，并@到新加的人。

### 29.2 根因说明（对应你反馈的现象）
- 分派 Webhook 事件原先只带了单个 `assigneeId`（主处理人），没有“当前处理人列表”。
- 追加处理人时，消息正文“指派给”仍按单人字段解析，导致看到旧值。
- @名单也主要基于单人字段收集，新增协同处理人可能漏掉。

### 29.3 本次修复内容
1. `TicketWebhookEventListener` 在分派事件中补充 `assigneeIds`（当前有效处理人列表，来自 `ticket_assignee`）。
2. `WebhookDispatchService` 在组装“变更内容”时：
   - 分派事件优先使用 `assigneeIds` 生成“指派给：A、B、C”；
   - 无列表时再回退旧逻辑（单个 `assigneeId`）。
3. `collectMentionTargets(...)` 收集@对象时，新增读取 `assigneeIds`，把当前处理人都纳入@候选。

### 29.4 使用方法（验收步骤）
1. 先让工单有主处理人A。
2. 在工单详情执行“追加处理人”，追加B。
3. 到企业微信群查看“工单事件通知”。
4. 预期结果：
   - 变更内容中的“指派给”包含B（例如：`A、B`）；
   - 群消息会真实@到B（若缺企微userid则走手机号兜底@）。

### 29.5 参数说明（本次修复相关）
| 字段 | 类型 | 说明 |
|---|---|---|
| `data.assigneeIds` | `Long[]` | 工单当前处理人列表（主处理人 + 协同处理人） |
| `data.assigneeId` | `Long` | 主处理人ID（保留兼容） |
| `buildChangeLines(data, eventType)` | `Object, WebhookEventType -> List<String>` | 分派事件优先按 `assigneeIds` 组装“指派给”文案 |
| `collectMentionTargets(ticket, data)` | `JSONObject, Object -> MentionTargets` | @对象收集，新增读取 `assigneeIds` |

### 29.6 返回值说明（接口行为）
| 场景 | 返回值 | 说明 |
|---|---|---|
| 分派事件走企微机器人Webhook | `msgtype=text` JSON | 文案“指派给”展示最新处理人列表，@名单包含追加人 |
| 分派事件无 `assigneeIds`（历史兼容） | `msgtype=text` JSON | 自动回退单人 `assigneeId`，保持老数据可用 |

### 29.7 常见问题（新增）
#### Q30：为什么“指派给”还是只显示1个人？
- **检测**：查看 `webhook_dispatch_log` 请求体中的 `data.assigneeIds` 是否存在且有值。
- **记录（错误类型）**：分派事件未携带处理人列表，或 `ticket_assignee` 明细未同步成功。
- **恢复建议**：
  1. 确认后端已升级到本次版本；
  2. 复查该工单 `ticket_assignee` 明细是否包含新增处理人；
  3. 重新执行一次“追加处理人”触发通知。

#### Q31：为什么还是没有@到新增处理人？
- **检测**：确认请求体是否包含新增人的 `mentioned_list` 或 `mentioned_mobile_list` 来源数据。
- **记录（错误类型）**：用户资料缺少可用 `wecom_userid` 且手机号无效，导致无法命中@。
- **恢复建议**：
  1. 优先补齐新增处理人的 `wecom_userid`；
  2. 或补齐合法手机号用于兜底@；
  3. 再次触发分派通知验证。

### 29.8 版本历史（新增）
| 版本 | 变更内容 |
|---|---|
| `v1.3.0-append-assignee-webhook-mention-fix` | 修复追加处理人后工单通知“指派给”不更新且未@新增处理人：分派Webhook补充 `assigneeIds`，文案与@名单统一按当前处理人列表生成 |

---

## 30. 企业微信日报“待解决有数量但明细全无”修复（后端）

### 30.1 功能用途
- **用途**：修复企业微信日报里“汇总显示待解决 10 个，但 1.1/1.2/1.3 都是无”的错位问题。  
- **类比理解**：以前像“购物车角标写有 10 件，但点开列表一件都看不到”；现在改成“角标和列表用同一份数据”，数字和内容一定对得上。

### 30.2 根因说明（对应你反馈的现象）
- 日报顶部“待解决”数字，原来按“全量未关闭状态”在算；
- 日报下方“待解决明细”，只展示了 3 个子分类（测试复现中/处理中/待简报）；
- 两边不是同一口径，导致出现“有总数、没明细”。

### 30.3 本次修复内容
1. `DailyReportApplicationService` 中，`summary.pendingResolveCount` 改为直接使用 `pendingSection.count`（即 1.1 + 1.2 + 1.3 合计）。
2. `DailyReportMapper.xml` 中，待简报查询从单状态 `pending_verify` 扩展为：
   - `pending_verify`
   - `pending_cs_confirm`（历史状态兼容）
3. 不改接口结构，不改前端字段，管理端预览与企微推送直接受益。

### 30.4 使用方法（验收步骤）
1. 调用 `/api/daily-report/preview` 生成当天日报。
2. 核对 `summary.pendingResolveCount` 与 `pendingSection.count`。
3. 核对 markdown 文本里“待解决问题：X个”与 1.1/1.2/1.3 明细合计。
4. 手动推送到企业微信群，确认群内文案与管理端预览一致。

### 30.5 参数说明（本次修复相关）
| 字段/查询 | 类型 | 说明 |
|---|---|---|
| `summary.pendingResolveCount` | `long` | 待解决汇总，现改为与待解决明细同口径 |
| `pendingSection.count` | `long` | 待解决分区总数（测试复现中 + 处理中 + 待简报） |
| `selectPendingVerifyTickets` | SQL 查询 | 待简报明细查询，兼容 `pending_verify` 与 `pending_cs_confirm` |

### 30.6 返回值说明（接口行为）
| 接口 | 返回值 | 说明 |
|---|---|---|
| `GET /api/daily-report/preview` | `ApiResult<DailyReportOutput>` | `summary.pendingResolveCount` 与 `pendingSection` 统计口径一致 |
| `POST /api/daily-report/push` | `ApiResult<Void>` | 推送到企微的 markdown 与预览数据保持一致 |

### 30.7 常见问题（新增）
#### Q32：为什么以前显示待解决 10 个，现在变少了？
- **检测**：查看 `pendingSection` 三个子分类合计是否与顶部一致。
- **记录（错误类型）**：历史版本使用“汇总和明细不同口径”导致数字偏大。
- **恢复建议**：
  1. 以新版本的同口径统计为准；
  2. 若需要扩展“待解决”分类（如待受理/待分派），单独做产品口径评审后再加。

#### Q33：为什么待简报还是有漏单？
- **检测**：确认工单状态是否为 `pending_verify` 或 `pending_cs_confirm`。
- **记录（错误类型）**：状态不在当前日报“待简报”定义范围内。
- **恢复建议**：
  1. 先核对工单真实状态；
  2. 如业务要纳入更多状态，走日报口径变更流程统一扩展。

### 30.8 版本历史（新增）
| 版本 | 变更内容 |
|---|---|
| `v1.3.1-daily-report-pending-alignment-fix` | 修复企业微信日报“待解决汇总与明细不一致”：汇总改为明细同口径合计，并兼容待简报历史状态 `pending_cs_confirm` |

---

## 31. 企业微信工单事件通知时间少 8 小时修复（后端）

### 31.1 功能用途
- **用途**：修复企业微信群里工单事件通知的“时间/变更时间”显示比实际操作时间少 8 小时的问题。  
- **类比理解**：以前像“手表按伦敦时间走，但你人在上海”；现在改成“手表自动读系统时区”，显示和你实际操作时间一致。

### 31.2 根因说明（对应你反馈的现象）
- `WebhookDispatchService` 里事件时间由 `formatNow()` 生成；
- 旧逻辑使用了服务器默认时区格式化（云主机常见是 UTC）；
- 所以你在 `09:29:44` 操作，通知里会显示 `01:29:44`。

### 31.3 本次修复内容
1. `WebhookDispatchService` 注入 `SystemConfigMapper`，读取基础配置 `BASIC/timezone`。
2. `formatNow()` 改为按业务时区格式化：
   - 优先使用系统配置时区；
   - 配置缺失或非法时，回退 `Asia/Shanghai`。
3. 这样 `[时间]` 与 `变更时间` 都走同一时区口径。

### 31.4 使用方法（验收步骤）
1. 在系统基础设置确认时区（例如 `Asia/Shanghai`）。
2. 执行一次工单状态变更（例如“开发解决中 -> 已完成”）。
3. 查看企业微信群消息中的：
   - `【时间】`
   - `变更时间`
4. 预期结果：显示与你实际操作一致（例如 `2026-04-10 09:29:44`）。

### 31.5 参数说明（本次修复相关）
| 字段/配置 | 类型 | 说明 |
|---|---|---|
| `system_config.config_group` | `String` | 固定 `BASIC` |
| `system_config.config_key` | `String` | 固定 `timezone` |
| `WebhookDispatchService.DEFAULT_TIMEZONE` | `String` | 时区兜底值，`Asia/Shanghai` |
| `WebhookDispatchService.formatNow()` | `String` | 现在按业务时区格式化当前时间 |

### 31.6 返回值说明（接口行为）
| 场景 | 返回值 | 说明 |
|---|---|---|
| Webhook 事件推送 | `msgtype=text` JSON | 文本中的 `【时间】/变更时间` 按业务时区输出 |
| 时区配置非法 | 日志告警 + 正常推送 | 回退 `Asia/Shanghai`，不阻断通知 |

### 31.7 常见问题（新增）
#### Q34：为什么通知时间还是不对？
- **检测**：先看系统基础设置里的时区值（是否为 `Asia/Shanghai` 或你期望时区）。
- **记录（错误类型）**：时区配置错误或未生效（缓存/未重启）。
- **恢复建议**：
  1. 把 `BASIC/timezone` 改成正确值；
  2. 重启后端服务；
  3. 再执行一次状态变更复测。

### 31.8 版本历史（新增）
| 版本 | 变更内容 |
|---|---|
| `v1.3.2-wecom-event-timezone-fix` | 修复企业微信工单事件通知时间少 8 小时：时间格式化改为按系统配置时区输出（默认 `Asia/Shanghai`） |

---

## 32. Bug简报编辑页“缺陷分类下拉”和“右侧说明”不一致修复（前端）

### 32.1 功能用途
- **用途**：修复 Bug 简报编辑页中“缺陷分类下拉选项”和右侧“缺陷分类说明”内容不一致的问题。  
- **类比理解**：以前像“菜单上的菜名”和“墙上价目表”不是同一份清单；现在两边都读同一张清单，永远同步。

### 32.2 根因说明（对应你反馈的现象）
- 下拉框使用的是后端字典（动态数据）；
- 右侧说明使用的是组件内写死的静态数组；
- 两个数据源独立维护，时间一长就会漂移。

### 32.3 本次修复内容
1. `BugReportInstructionContent.vue` 新增 `defectCategories` 入参；
2. 右侧“缺陷分类说明”改为由 `defectCategories` 动态渲染；
3. 删除原本写死的 `DEFECT_CATEGORY_ITEMS` 静态清单；
4. `BugReportEditView.vue` 在 sidebar/drawer 两个入口都传入同一份 `defectCategories`，与下拉框同源。

### 32.4 使用方法（验收步骤）
1. 进入「Bug简报编辑」页面；
2. 打开“缺陷分类”下拉，记下分类名称列表；
3. 对照右侧“缺陷分类”说明列表；
4. 预期结果：两边名称、顺序保持一致。

### 32.5 参数说明（本次修复相关）
| 参数/字段 | 类型 | 说明 |
|---|---|---|
| `BugReportInstructionContent.defectCategories` | `DefectCategoryOutput[]` | 右侧说明使用的分类数据源，和下拉同源 |
| `DefectCategoryOutput.name` | `string` | 分类名称（用于列表与下拉展示） |
| `DefectCategoryOutput.description` | `string` | 分类说明文案（为空时显示“暂无说明”） |

### 32.6 返回值说明（页面行为）
| 场景 | 返回值 | 说明 |
|---|---|---|
| 打开编辑页 | `void` | 下拉与右侧说明都使用同一份缺陷分类字典渲染 |
| 字典为空 | `void` | 右侧显示“暂无缺陷分类说明”占位，不显示过期静态内容 |

### 32.7 常见问题（新增）
#### Q35：为什么我改了缺陷分类字典，右侧说明没变化？
- **检测**：确认页面是否刷新到最新字典（本地缓存/旧会话）。
- **记录（错误类型）**：前端缓存导致旧数据未重新拉取。
- **恢复建议**：
  1. 强制刷新页面（`Ctrl+F5`）；
  2. 重新进入 Bug 简报编辑页；
  3. 如仍异常，检查字典接口返回是否已更新。

### 32.8 版本历史（新增）
| 版本 | 变更内容 |
|---|---|
| `v1.3.3-bugreport-defect-category-instruction-sync` | 修复 Bug 简报编辑页缺陷分类口径不一致：右侧说明改为与下拉同源的动态字典渲染，避免静态列表漂移 |

---

## 33. Bug简报“缺陷分类”升级为16类（字典扩展）

### 33.1 功能用途
- **用途**：把 Bug 简报中的“缺陷分类”从 7 类升级到 16 类，分类更细，复盘和统计更准确。  
- **类比理解**：以前像“垃圾只分干湿两类”，很多东西会混在一起；现在像“可回收/厨余/有害/其他+细分”，分类更清楚，后续统计更有价值。

### 33.2 本次改动了什么
1. 新增数据库迁移：`V48__expand_bugreport_defect_category_to_16.sql`。  
2. 将旧的 7 个分类设置为停用（`is_active=0`）。  
3. 新增并启用 16 个分类（`is_active=1`，`sort_order=1..16`）：
   - 页面异常
   - 交互异常
   - 设计问题
   - 错误逻辑
   - 错误代码
   - 错误数据
   - 错误配置
   - 错误发布
   - 基础服务
   - 基础设施
   - 兼容问题
   - 架构问题
   - 性能问题
   - 安全问题
   - 安装部署
   - 第三方原因
4. 前端无需改接口：编辑页下拉和右侧说明都继续走 API000030（同一份字典数据）。

4. 16类说明文案精确对齐业务口径（用于下拉与右侧说明同源展示）：
   1) 页面异常：由于页面排版不合理、样式、文案错误，页面显示重叠等导致的页面性错误。  
   2) 交互异常：由于前端用户交互体验、接口调用导致的错误。  
   3) 设计问题：逻辑删除无“确认窗”、用户体验感低、注释描述有歧义或存在错别字、相关提示语缺失、光标位置不正确、文字排列不整齐，相关功能场景未考虑等。  
   4) 错误逻辑：由于业务逻辑、代码逻辑异常导致的错误。  
   5) 错误代码：逻辑正确的情况下由于编码问题导致的错误。  
   6) 错误数据：由于数据与业务不兼容导致的错误。  
   7) 错误配置：由于配置文件异常导致（参数配置错误等）的错误。  
   8) 错误发布：由于灰度或线上发布流程异常导致的错误。  
   9) 基础服务：由于基础组件、服务网关、任务调度、ES 搜索引擎等服务故障导致的错误。  
   10) 基础设施：数据库相关（如数据库配置、数据库定时作业运行故障、数据库脚本操作、数据库高可用性架构等）、缓存 Redis 相关、Nginx 反向代理、web 负载均衡等。  
   11) 兼容问题：由于浏览器（不同的浏览器、或者浏览器版本太低）、移动设备或数据库版本不兼容导致的错误。  
   12) 架构问题：由于架构不适用导致的错误，如无法支持大数据量存储、系统扩展性差等。  
   13) 性能问题：并发产生的问题、页面响应时间长、服务器资源耗用过高、死锁、内存泄露、难以满足系统性能要求等。  
   14) 安全问题：账号越权、sql 注入漏洞、频率未做限制（比如短信发送次数，登录密码次数未做限制）等。  
   15) 安装部署：服务器初始化（操作系统安装，基础环境支持）、软件安装（比如数据库管理系统，ES 程序等）、编译后代码部署等产生的问题。  
   16) 第三方原因：由于第三方服务异常导致的错误。如：腾讯云服务器网络、七牛云存储、大禹高防及 SSL 证书等。

### 33.3 使用方法（验收步骤）
1. 启动后端并完成 Flyway 迁移。  
2. 打开「Bug简报编辑」页面。  
3. 点击“缺陷分类”下拉。  
4. 预期结果：看到上面 16 个分类，顺序一致；右侧“缺陷分类说明”同步一致。

### 33.4 参数说明（字典接口）
| 参数/字段 | 类型 | 说明 |
|---|---|---|
| `GET /api/dict/defect-category` | API | 缺陷分类字典接口（API000030） |
| `id` | `number` | 字典主键 |
| `name` | `string` | 缺陷分类名称 |
| `description` | `string` | 缺陷分类说明 |

### 33.5 返回值说明
| 场景 | 返回值 | 说明 |
|---|---|---|
| 正常请求字典 | `DefectCategoryOutput[]` | 返回 16 条启用分类，按 `sort_order` 排序 |
| 历史简报详情 | `defectCategory: string` | 历史记录仍按当时文本显示，不受字典停用影响 |

### 33.6 错误处理三步法（排查建议）
#### Q36：为什么下拉里还是旧分类？
- **检测**：先查数据库 `dict_defect_category`，看新16类是否已写入并启用。  
- **记录（错误类型）**：常见是迁移未执行、环境连接错库、前端缓存未刷新。  
- **恢复建议**：
  1. 确认后端启动日志里 `V48/V49` 已成功执行；
  2. 确认当前环境数据库与后端连接一致；
  3. 前端强刷（`Ctrl+F5`）后再进入编辑页。

### 33.7 版本历史（新增）
| 版本 | 变更内容 |
|---|---|
| `v1.3.4-bugreport-defect-category-16` | Bug简报缺陷分类从7类升级为16类（字典扩展）；旧分类停用，新分类按统一顺序返回，前端下拉与右侧说明自动同步 |
| `v1.3.6-bugreport-defect-category-16-wording-align` | 缺陷分类16类描述文案与业务口径精确对齐（包含页面性错误、确认窗、基础设施等措辞），下拉与右侧说明同源一致 |
| `v1.3.7-cd-deploy-trigger-flyway-version-fix` | 修复“构建成功但线上不生效”：恢复 CD `testVersion` 同步触发链路，并消除 Flyway 重复版本号冲突（Bug简报迁移改为 V48/V49） |
| `v1.3.8-flyway-v44-sso-session-version-fix` | 继续消除 Flyway 版本冲突：将 `V44__init_sso_session.sql` 调整为 `V50__init_sso_session.sql`，避免与视频附件迁移重复编号导致启动迁移失败 |

---

## 34. Bug简报“逻辑归因下拉”与右侧说明12类统一（前端）

### 34.1 功能用途
- **用途**：让 Bug简报编辑页里的“逻辑归因下拉”与右侧“逻辑归因说明”完全一致，统一为右侧的12类。  
- **类比理解**：以前像“试卷选项”和“答案解析”不是同一套题；现在两者都用同一张题库，用户不会选错方向。

### 34.2 本次改动了什么
1. 新增逻辑归因统一常量：`src/constants/bugreport-logic-cause.ts`。  
2. 右侧说明组件 `BugReportInstructionContent.vue` 改为读取统一常量渲染。  
3. 编辑页 `BugReportEditView.vue` 的逻辑归因输入，从“级联树”改为“单选下拉”（12项，与右侧同源）。  
4. 提交参数保持接口兼容：
   - `logicCauseLevel1` 写入选中的12类名称；
   - `logicCauseLevel2` 固定不提交（`undefined`）。

### 34.3 逻辑归因12类（与右侧一致）
1. 逻辑错误  
2. 数据处理错误  
3. 异常处理不当  
4. 接口使用错误  
5. 资源管理问题  
6. 并发问题  
7. 配置错误  
8. 注释与代码不一致  
9. 兼容性问题  
10. 安全漏洞  
11. 性能问题  
12. 重构风险

### 34.4 使用方法（验收步骤）
1. 打开「Bug简报编辑」页面。  
2. 点击“逻辑归因”下拉框。  
3. 对照右侧“逻辑归因”说明列表。  
4. 预期结果：下拉项名称、顺序与右侧说明一致（12项）。

### 34.5 参数说明（本次改动相关）
| 参数/字段 | 类型 | 说明 |
|---|---|---|
| `BUGREPORT_LOGIC_CAUSE_GUIDE` | `LogicCauseGuideItem[]` | 逻辑归因说明统一数据源（12类） |
| `BUGREPORT_LOGIC_CAUSE_OPTIONS` | `LogicCauseSelectOption[]` | 逻辑归因下拉选项，基于 GUIDE 映射 |
| `logicCauseLevel1` | `string` | 保存所选逻辑归因（12类之一） |
| `logicCauseLevel2` | `string \| undefined` | 当前页面不再选择二级，提交时为空 |

### 34.6 返回值说明（页面行为）
| 场景 | 返回值 | 说明 |
|---|---|---|
| 选择逻辑归因 | `void` | 页面展示单选结果，并用于保存/提交 |
| 清空逻辑归因 | `void` | 逻辑归因字段置空，不影响其他字段 |

### 34.7 错误处理三步法（排查建议）
#### Q37：为什么下拉项和右侧说明还不一致？
- **检测**：确认页面是否加载到最新前端包，并检查 `bugreport-logic-cause.ts` 是否被正确打包。  
- **记录（错误类型）**：常见是浏览器缓存旧包或发布未生效。  
- **恢复建议**：
  1. 强刷页面（`Ctrl+F5`）；
  2. 确认本次前端版本已发布；
  3. 重新进入编辑页复查12项是否一致。

### 34.8 版本历史（新增）
| 版本 | 变更内容 |
|---|---|
| `v1.3.5-bugreport-logic-cause-12-sync` | 逻辑归因下拉与右侧说明统一为同源12类：页面选择项与说明口径一致，避免“选项/说明”漂移 |

---

## 35. 构建成功但线上未生效（CD触发链路 + Flyway版本冲突）修复说明

### 35.1 功能用途
- **用途**：修复“GitHub Actions 显示构建成功，但线上页面还是旧版本”的问题。  
- **类比理解**：以前像“厨房把菜做好了，但服务员没端上桌”；这次把“端菜动作”补齐，同时修掉“后厨出餐编号冲突”。

### 35.2 根因（两条主线）
1. **CD 触发链路断点**：`cd.yml` 中 `testVersion` 同步逻辑曾被注释，导致部分环境只更新制品，不触发部署消费。  
2. **Flyway 版本冲突**：迁移脚本存在重复版本号（`V44`、`V46`），新镜像启动时可能在迁移阶段失败，实例回不到新版本。

### 35.3 本次修复内容
1. **恢复并加固 CD 触发写入**（`.github/workflows/cd.yml`）  
   - 恢复 `testVersion` 同步；  
   - 增加“文件存在 / 键存在”兜底，避免不同仓库结构导致脚本报错；  
   - 无 `.cnb/web_trigger.yml` 时仅告警并继续，不阻断 `.cnb_version.yml` 更新。
2. **消除 Flyway 重复版本号**（`ticket-platform/.../db/migration`）  
   - `V46__expand_bugreport_defect_category_to_16.sql` → `V48__...`  
   - `V47__align_bugreport_defect_category_wording.sql` → `V49__...`  
   - `V44__init_sso_session.sql` → `V50__...`

### 35.4 使用方法（验收步骤）
1. 合并代码后观察 `CD – Build & Push Docker Images`，确认 `Update CNB Version File` 成功。  
2. 在版本仓库检查：
   - `.cnb_version.yml` 的 `version` 是否为本次 `sha-xxxxxxx`；  
   - `.cnb/web_trigger.yml` 的 `testVersion` 是否同步更新。  
3. 在后端日志检查 Flyway：不再出现 duplicate version 报错。  
4. 强刷前端页面（`Ctrl+F5`），确认业务改动可见。

### 35.5 参数说明（本次修复相关）
| 项 | 类型 | 说明 |
|---|---|---|
| `.cnb/web_trigger.yml testVersion` | `string` | 部署触发消费版本号，需与本次构建版本一致 |
| `.cnb_version.yml version` | `string` | 制品版本索引（例如 `sha-8559782`） |
| Flyway migration version | `int` | 必须全局唯一递增，禁止重复 |

### 35.6 返回值说明（链路行为）
| 场景 | 返回值 | 说明 |
|---|---|---|
| CD 版本写入成功 | `git commit + push` | 版本仓库会新增提交，部署系统可感知新版本 |
| 缺少 `.cnb/web_trigger.yml` | `WARN日志` | 不阻断流程，但不会更新 `testVersion` |
| Flyway 启动迁移 | 启动成功/失败日志 | 本次修复后应避免重复版本导致的失败 |

### 35.7 常见问题（新增）
#### Q38：为什么 CD 成功了，页面还是旧的？
- **检测**：先看版本仓库里的 `testVersion` 和 `.cnb_version.yml` 是否已更新到本次 SHA。  
- **记录（错误类型）**：部署触发链路未消费新版本，或后端实例迁移失败回退旧版本。  
- **恢复建议**：
  1. 手动核对部署系统当前运行镜像 tag；
  2. 查看后端启动日志中 Flyway 执行结果；
  3. 前端强刷缓存后再验证页面。

#### Q39：为什么后端启动时报 Flyway duplicate version？
- **检测**：查看 `db/migration` 是否存在同一版本号多个脚本。  
- **记录（错误类型）**：迁移脚本版本冲突。  
- **恢复建议**：
  1. 保证每个迁移版本号全局唯一；
  2. 用新增版本号迁移替代，不要在已发布环境直接改历史脚本内容。

### 35.8 版本历史（新增）
| 版本 | 变更内容 |
|---|---|
| `v1.3.7-cd-deploy-trigger-flyway-version-fix` | 修复“构建成功但线上不生效”：恢复 CD testVersion 同步触发链路，并消除 Flyway 重复版本号冲突（迁移改为 V48/V49/V50） |

---

## 36. Bug 简报「关联工单」仅展示可写简报的工单（前后端）

### 36.1 功能用途
- **用途**：在 Bug 简报新建/编辑页，「关联工单」搜索下拉只出现**临时解决**或**已完成**工单；**已关闭**（多指非缺陷关闭）不出现在列表中且保存时不可关联。保存简报时也会拒绝其它状态的工单 ID，避免接口被绕过。
- **类比**：以前像「通讯录里所有人都能被选为紧急联系人」；现在只有「缺陷已临时稳住」或「缺陷已真正修完结单」的人才能被选。

### 36.2 使用方法（验收步骤）
1. 打开「新建 Bug 简报」或编辑草稿；
2. 在「关联工单」输入框搜索或展开列表；
3. 预期：列表中不出现「处理中」等未结单状态工单；
4. 若用接口直接提交非法状态工单 ID，预期：接口返回明确错误提示。

### 36.3 参数说明
| 参数 | 类型 | 说明 |
|---|---|---|
| `GET /api/ticket/page` 查询参数 `linkableForBugReport` | `boolean` | 传 `true` 时仅返回 `temp_resolved`、`completed` 工单 |
| Bug 简报 `ticketIds` | `number[]` | 每个 ID 对应工单状态须为上述两种之一 |

### 36.4 版本历史（新增）
| 版本 | 变更内容 |
|---|---|
| `cursor/bug-report-ticket-status-filter-8653` | 工单分页支持 `linkableForBugReport`（仅 `temp_resolved`/`completed`）；简报保存校验；编辑/详情/提示文案与「非缺陷关闭不写简报」口径一致 |

---

## 37. Bug 简报编辑页「审核人」提示与宽度（前端）

### 37.1 功能用途
- **用途**：去掉「审核人」下方的红色「P0/P1 必须审核」提示（严重级别说明区已覆盖规则）；并让「审核人」下拉与「反馈人」等同一行内占满表单项宽度，避免选前选后框体宽度不一致。

### 37.2 使用方法（验收步骤）
1. 打开「Bug 简报编辑」，将严重级别选为需要填写审核人的档位（P0/P1/P2）。
2. 确认「审核人」下方不再出现「P0/P1 必须审核」标签。
3. 未选、已选、清空后，「审核人」输入框横向宽度应与同区块其他下拉一致。

### 37.3 参数说明
| 项 | 类型 | 说明 |
|---|---|---|
| `BugReportEditView.vue` 审核人表单项 | 模板 | 与其它 `el-select` 一致，直接置于 `el-form-item` 内容区，宽度由表单区 `width: 100%` 规则统一 |

### 37.4 版本历史（新增）
| 版本 | 变更内容 |
|---|---|
| `cursor/bug-report-reviewer-ui-6c51` | 移除审核人下方提示标签；去掉多余包裹层，统一审核人下拉宽度表现 |

---

## 38. 工单详情“处理完成/临时解决确认”后直达 Bug 简报编辑（前端）

### 38.1 功能用途
- **用途**：在工单详情页点击「处理完成」或「临时解决确认」后，不再停留当前页提示用户手动找入口，而是直接跳转到 Bug 简报编辑页，并自动关联当前工单。  
- **类比理解**：以前像“办完手续后还要自己去另一个窗口排队”；现在像“柜员直接把你带到下一个窗口，并把你的资料一起带过去”。

### 38.2 使用方法（验收步骤）
1. 打开任意缺陷工单详情页。  
2. 执行动作「处理完成」或「临时解决确认」（兼容历史动作名「验证完成」）。  
3. 预期：操作成功后直接进入 Bug 简报编辑页。  
4. 在编辑页检查「关联工单」：应自动包含当前工单，无需手动再搜一遍。  
5. 若该工单已存在“待填写”简报草稿，预期直接进入该草稿编辑页；否则进入新建编辑页并自动带入该工单。

### 38.3 参数说明
| 参数 | 类型 | 说明 |
|---|---|---|
| `GET /ticket/detail/:id` 里的流转动作名 | `string` | 命中「处理完成 / 临时解决确认 / 验证完成」时触发自动跳转 |
| 跳转查询参数 `ticketId` | `string` | 从工单详情页跳到 `/bug-report/edit` 时携带当前工单 ID |
| 跳转查询参数 `ticketIds` | `string \| string[]` | 简报编辑页兼容多工单预关联（逗号分隔或多参数） |

### 38.4 返回值说明（页面行为）
| 场景 | 返回值 | 说明 |
|---|---|---|
| 处理完成/临时解决确认成功 | `router.push('/bug-report/edit/...')` | 自动进入简报编辑链路 |
| 已有待填写简报草稿 | `router.push('/bug-report/edit/{reportId}')` | 直接打开草稿并继续补填 |
| 无待填写草稿 | `router.push('/bug-report/edit?ticketId={id}')` | 新建简报时自动预关联当前工单 |

### 38.5 常见问题（新增）
#### Q40：为什么跳到简报编辑后没有看到自动关联工单？
- **检测**：先看地址栏是否包含 `ticketId` 或 `ticketIds`。  
- **记录（错误类型）**：路由参数缺失或当前简报是已有草稿但已关联了其它工单。  
- **恢复建议**：
  1. 重新从工单详情执行「处理完成/临时解决确认」触发跳转；
  2. 确认前端版本已更新并强刷缓存（`Ctrl+F5`）；
  3. 若是编辑已有草稿，检查该草稿的关联工单列表是否符合当前业务预期。

## 38. 工单「临时解决」状态也展示关联 Bug 简报（后端）

### 38.1 功能用途
- **用途**：让缺陷工单进入「临时解决」后，工单详情页也能看到「关联 Bug 简报」并可直接跳转填写。  
- **类比理解**：以前像“到了中转站却看不到包裹信息”；现在到“临时解决”这个中转站时，也会自动把包裹单挂上去，方便后续继续处理。

### 38.2 使用方法（验收步骤）
1. 选择一条缺陷工单，确保当前没有已关联的 Bug 简报。  
2. 在工单详情执行流转，把状态改为「临时解决（temp_resolved）」。
3. 刷新工单详情页。  
4. 预期结果：
   - 详情页出现「关联 Bug 简报」区块；
   - 区块中能看到简报编号和“查看简报”入口；
   - 重复进入临时解决不会重复生成多条自动简报关联。

### 38.3 参数说明（本次改动相关）
| 参数/事件 | 类型 | 说明 |
|---|---|---|
| `TicketStatusChangedEvent.newStatus` | `String` | 当值为 `temp_resolved` 时触发自动创建/复用简报关联 |
| `BugReportApplicationService.createDraftFromClosedTicket(ticketId, operatorId)` | `method` | 复用现有幂等逻辑：有关联则复用，无关联则创建草稿并建立关联 |
| `TicketCompletedEvent.finalStatus` | `String` | 既有逻辑保持不变：`closed` 不自动建简报，`completed` 等终态继续自动建 |

### 38.4 返回值说明（接口行为）
| 场景 | 返回值 | 说明 |
|---|---|---|
| 工单流转到 `temp_resolved` | `void`（事件监听） | 事务提交后异步触发简报关联创建/复用 |
| 获取工单详情 `GET /api/ticket/detail/{id}` | `TicketDetailOutput` | `bugReports` 在临时解决状态下可返回关联简报数据 |

### 38.5 常见问题（新增）
#### Q40：为什么临时解决后还是看不到关联 Bug 简报？
- **检测**：先确认该工单是否是缺陷工单，并且流转后状态确实为 `temp_resolved`。  
- **记录（错误类型）**：常见是状态未真正流转到临时解决，或后端版本仍是旧逻辑。  
- **恢复建议**：
  1. 升级到包含本修复的版本；
  2. 重新执行一次“临时解决”流转；
  3. 再刷新工单详情确认 `bugReports` 是否有数据。

### 38.6 版本历史（新增）
| 版本 | 变更内容 |
|---|---|
| `cursor/ticket-detail-to-bugreport-edit-5799` | 工单详情执行「处理完成/临时解决确认」后自动跳转 Bug 简报编辑，并自动预关联当前工单 |
| `v1.3.9-temp-resolved-bug-briefing-visible` | 新增临时解决状态自动建立/复用 Bug 简报关联，确保工单详情在临时解决阶段也展示「关联 Bug 简报」 |
