spring:
  application:
    name: ticket-platform
  cloud:
    nacos:
      config:
        enabled: true
        server-addr: ${NACOS_SERVER_ADDR:http://10.0.4.4:8848}
        file-extension: yaml
        namespace: ${NACOS_NAMESPACE:}
