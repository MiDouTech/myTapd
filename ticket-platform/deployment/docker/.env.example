# ============================================================
# 开发环境 docker-compose 配置模板
# 执行：cp .env.example .env，然后填写所有密码值
# 注意：.env 文件绝对不能提交到 git
# ============================================================

# MySQL
MYSQL_ROOT_PASSWORD=change_me_to_a_strong_password

# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=change_me_to_a_strong_password
