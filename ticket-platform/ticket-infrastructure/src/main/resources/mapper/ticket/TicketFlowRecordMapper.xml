<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.miduo.cloud.ticket.infrastructure.persistence.mybatis.ticket.mapper.TicketFlowRecordMapper">

    <!-- 查询工单流转历史（时间正序，最早的排在最前） -->
    <select id="selectByTicketId"
            resultType="com.miduo.cloud.ticket.infrastructure.persistence.mybatis.ticket.po.TicketFlowRecordPO">
        SELECT
            id,
            ticket_id,
            ticket_no,
            flow_type,
            transition_id,
            transition_name,
            from_status,
            to_status,
            from_assignee_id,
            to_assignee_id,
            operator_id,
            operator_role,
            remark,
            create_time,
            update_time,
            create_by,
            update_by,
            deleted
        FROM ticket_flow_record
        WHERE ticket_id = #{ticketId}
          AND deleted = 0
        ORDER BY create_time ASC
    </select>

</mapper>
