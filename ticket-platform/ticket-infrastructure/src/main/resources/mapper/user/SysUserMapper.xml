<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.miduo.cloud.ticket.infrastructure.persistence.mybatis.user.mapper.SysUserMapper">

    <select id="selectRoleCodesByUserId" resultType="java.lang.String">
        SELECT r.role_code
        FROM sys_user_role ur
        INNER JOIN sys_role r ON ur.role_id = r.id AND r.deleted = 0
        WHERE ur.user_id = #{userId}
          AND ur.deleted = 0
    </select>

    <select id="selectByDepartmentIds" resultType="com.miduo.cloud.ticket.infrastructure.persistence.mybatis.user.po.SysUserPO">
        SELECT *
        FROM sys_user
        WHERE department_id IN
        <foreach collection="departmentIds" item="deptId" open="(" separator="," close=")">
            #{deptId}
        </foreach>
        AND deleted = 0
        ORDER BY id ASC
    </select>

    <select id="selectRoleCodesByUserIds" resultType="com.miduo.cloud.ticket.infrastructure.persistence.mybatis.user.model.UserRoleCodePair">
        SELECT ur.user_id AS userId,
               r.role_code AS roleCode
        FROM sys_user_role ur
        INNER JOIN sys_role r ON ur.role_id = r.id AND r.deleted = 0
        WHERE ur.user_id IN
        <foreach collection="userIds" item="userId" open="(" separator="," close=")">
            #{userId}
        </foreach>
          AND ur.deleted = 0
    </select>

</mapper>
