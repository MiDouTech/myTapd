<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.miduo.cloud.ticket.infrastructure.persistence.mybatis.bugreport.mapper.BugReportResponsibleMapper">

    <insert id="batchInsert" parameterType="list">
        INSERT INTO bug_report_responsible (report_id, user_id, create_time, update_time, create_by, update_by, deleted)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.reportId}, #{item.userId},
             NOW(), NOW(), #{item.createBy}, #{item.updateBy}, 0)
        </foreach>
    </insert>

</mapper>
