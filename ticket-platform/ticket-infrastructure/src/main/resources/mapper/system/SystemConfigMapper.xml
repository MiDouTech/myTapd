<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.miduo.cloud.ticket.infrastructure.persistence.mybatis.system.mapper.SystemConfigMapper">

    <select id="selectByGroup" parameterType="java.lang.String"
            resultType="com.miduo.cloud.ticket.infrastructure.persistence.mybatis.system.po.SystemConfigPO">
        SELECT id, config_key, config_value, config_group, description,
               create_time, update_time, create_by, update_by, deleted
        FROM system_config
        WHERE config_group = #{configGroup}
          AND deleted = 0
    </select>

</mapper>
