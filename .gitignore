# Local nacos config with real secrets (never commit)
miduo-md/nacos-config/*.local
miduo-md/nacos-config/*.local.yml

# Environment files with secrets (never commit)
.env
.env.local
.env.*.local
*.env.local

# IDE and OS
.idea/
.DS_Store
