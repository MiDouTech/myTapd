# 实施任务列表：插件「我的工单」列表体验升级

## task001 — 定义 API 契约与 DTO

- [x] `PluginTicketMinePageInput` 增加 `title`、`categoryId`、`status` 及长度、正整数和枚举校验。
- [x] `PluginTicketSummaryOutput` 增加 `categoryId`、`categoryName`。
- [x] 新增分类/状态选项与专用分页输出 DTO；两个 options 字段默认为空数组。
- [x] 更新控制器返回类型，保持接口编号和原有路由不变。

**完成判定**：不传新参数时原调用可正常反序列化；新响应保留所有原分页与摘要字段。

---

## task002 — 实现后端筛选与分类聚合

- [x] 主分页查询附加标题、分类、状态可选条件，并保留三个强制归属条件。
- [x] 当前页分类名称使用一次批量查询补齐，不产生 N+1。
- [x] 增加不受当前筛选和分页影响的分类、状态选项聚合查询。
- [x] 对空分类、分类缺失和空名称应用「未分类」兜底策略。
- [x] 相同创建时间增加稳定的 `id DESC` 排序。

**完成判定**：服务端筛选准确，选项覆盖当前用户跨页工单分类，查询范围不会越权。

---

## task003 — 后端自动化测试

- [ ] 覆盖无参数兼容、单条件/组合搜索、LIKE 转义、空结果和非法状态。
- [ ] 覆盖同分类下不同应用/不同用户的数据隔离。
- [ ] 覆盖分类/状态选项去重、稳定排序、不随搜索条件收缩。
- [ ] 覆盖分类为空或已失效时的响应兜底。
- [ ] 验证批量映射，避免按记录逐条查询。

**完成判定**：相关模块测试通过，并能防止归属条件在后续重构中被误删。

---

## task004 — SDK 筛选与列表展示

- [x] 在 SDK 定义具名的列表页、摘要、分类/状态选项和查询状态类型。
- [x] 实现桌面五列列表和窄屏卡片布局，展示编号、标题、分类、状态、查看。
- [x] 增加标题输入框、分类和状态下拉框以及查询、重置操作。
- [x] `fetchMineTickets` 使用 `URLSearchParams` 传递分页和三个可选搜索参数。
- [x] 实现总数、当前页、上一页/下一页和 10/20/50 每页条数切换。
- [x] 实现空结果、错误重试和请求竞态保护。

**完成判定**：交互满足设计稿描述，快速切换不会出现旧结果覆盖新结果。

---

## task005 — 构建、发布与回归

- [ ] 执行后端相关测试及编译。
- [x] 执行 `ticket-sdk` 构建和 `publish:frontend`，确认前端静态产物已同步。
- [x] 执行前端 build。
- [x] SDK 暴露 `TicketSDK.version`，并加强 `/sdk/` 的浏览器及 CDN 禁止缓存响应头，便于确认线上实际加载版本。
- [ ] 在浏览器中回归组合搜索、翻页、长标题、窄屏和空/错状态，并保留截图。
- [ ] 核对生成产物只包含预期变化。

**完成判定**：后端、SDK、前端检查通过，源码和 `public/sdk/v1/ticket-sdk.min.js` 行为一致。
