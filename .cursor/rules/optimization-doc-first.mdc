---
alwaysApply: true
---
# 优化先行文档收敛规则

## 适用范围

凡标注为**优化**、**重构**、**性能改进**、**体验改进**、**技术债偿还**等**不改变对外契约的改进类工作**，在编写或修改业务代码之前，必须先完成文档收敛。

> 说明：若优化同时涉及新增/变更对外接口或产品行为，还须叠加 [api-interface-numbering.mdc](mdc:.cursor/rules/api-interface-numbering.mdc) 等既有规范。

## 强制顺序（不可颠倒）

1. **收敛变更需求**  
   - 在 `miduo-md/` 下用**既有文档体系**记录：背景、现状问题、目标、非目标、验收标准或衡量方式（可测指标优先）。  
   - 优先更新或增补：`miduo-md/business/`（需求侧）、`miduo-md/workflow/`（流程与任务侧）；必要时同步 `miduo-md/standard/`（规范侧）。  
   - 工单/内部平台等子项目若文档不在 `miduo-md/`，则在对应模块的 README 或 `doc/` 下**同等级别**落一份变更说明，并在规则或索引中可检索。

2. **更新设计方案**  
   - 在 `miduo-md/workflow/`（或该模块约定的设计文档目录）中更新：**方案概要、影响范围、关键决策、回滚或兼容策略**。  
   - 若涉及接口、数据模型、权限或配置，须在对应**模块 API/设计文档**中反映，并与产品/需求条目可追溯对应。

3. **再实施代码与配置变更**  
   - 文档与方案评审或自检通过后，再改代码、脚本与配置。  
   - 若实施中发现方案与文档不一致，**先改文档再改代码**，禁止仅改代码不留痕。

## 例外（仍需补文档）

- **紧急线上修复**：可先 hotfix，但在合入主线或关闭事项前**必须补全**上述「变更需求 + 设计方案」记录，并注明紧急原因与时间线。

## 自检清单

- [ ] 变更需求已写入指定目录，目标与验收清晰  
- [ ] 设计方案已更新，范围与风险已写明  
- [ ] 代码/配置变更与文档描述一致  

---

**原则**：所有优化最终都要**先收敛文档**（记录变更需求、更新设计方案），再动实现；文档是优化的**单一事实来源**之一，与代码同等重要。
