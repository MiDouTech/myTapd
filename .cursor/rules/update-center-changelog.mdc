---
alwaysApply: true
---
# 更新中心变更记录同步规则

## 核心要求

后续每次完成代码、配置、脚本或文档变更后，必须同步更新更新中心内容。

可以把它理解成：  
代码提交是“脚印”，只会出现在「GitHub提交」；更新日志是“公告”，需要主动写入对应 Markdown 文件，才会出现在「已发布 / 待发布」。

## 写入位置

### 1. 未正式发布的改动

写入 `changelogs/*.md`：

```text
changelogs/YYYY-MM-DD_简短英文说明.md
```

示例：

```markdown
# 更新中心修复

| 类型 | 模块 | 描述 |
|---|---|---|
| fix | 更新中心 | 修复 GitHub raw 文件读取失败时更新记录为空的问题 |
```

### 2. 已正式发布的改动

写入 `CHANGELOG.md` 对应版本块，并删除已经归档的 `changelogs/*.md` 碎片。

## 表格格式

必须使用三列表格：

```markdown
| 类型 | 模块 | 描述 |
|---|---|---|
| feat | 更新中心 | 新增管理端更新中心页面 |
```

## 类型建议

| 类型 | 含义 |
|---|---|
| feat | 新功能 |
| fix | 修复 |
| perf | 优化 |
| refactor | 重构 |
| docs | 文档 |
| test | 测试 |
| chore | 杂项 |
| ops | 运维 |

## 自检清单

- [ ] 本次代码/配置/文档变更已写入 `changelogs/*.md` 或 `CHANGELOG.md`
- [ ] 表格列为 `类型 / 模块 / 描述`
- [ ] 如果变更已经发布，已从 `changelogs/*.md` 归档到 `CHANGELOG.md`
- [ ] PR 描述中说明了更新中心记录已同步
