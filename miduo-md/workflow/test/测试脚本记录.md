打开 http://localhost:5173/login?redirect=/dashboard，按 F12 控制台执行：
localStorage.setItem('ticket_access_token', '<YOUR_ACCESS_TOKEN>')
localStorage.setItem('ticket_refresh_token', '<YOUR_REFRESH_TOKEN>')
location.href = '/dashboard'

注意：请通过 dev-login 接口获取有效的 JWT token，不要在文档中存放真实 token。

cd /workspace/miduo-frontend
npm install
npm run dev

cd ticket-platform
mvn -DskipTests compile
mvn spring-boot:run -pl ticket-bootstrap
